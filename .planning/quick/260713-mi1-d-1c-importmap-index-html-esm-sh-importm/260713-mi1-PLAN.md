---
phase: quick-260713-mi1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - CLAUDE.md
  - .planning/codebase/STACK.md
autonomous: true
requirements: [D-1c]
tags: [importmap, esm.sh, vite, dependencies, docs]

must_haves:
  truths:
    - "index.html 無任何 esm.sh／importmap 字樣；Google Fonts link、root div、index.tsx module script 一字未動"
    - "npm ci 與 npm run build 在 importmap 移除後均成功（Vite 從 node_modules＋lockfile 解析五件套）"
    - "dist/index.html 內 esm.sh 出現次數 = 0（before 基線為 6 處）"
    - "CLAUDE.md 與 STACK.md 均描述依賴單軌（package.json＋package-lock.json），無殘留雙軌敘述"
    - "dist/ 內 grep AIza 無結果（金鑰紅線不因建置變動而破）"
  artifacts:
    - path: "index.html"
      provides: "純淨入口文件：meta、title、fonts link、root div、module script，無 script[type=importmap]"
    - path: "CLAUDE.md"
      provides: "「本專案關鍵事實」依賴條目改為單軌敘述"
      contains: "package-lock.json"
    - path: ".planning/codebase/STACK.md"
      provides: "Runtime／Styling／Configuration／Production 四處敘述更新為 bundle 單軌＋建置期 Tailwind"
  key_links:
    - from: "index.html"
      to: "index.tsx"
      via: "<script type=\"module\" src=\"/index.tsx\">（importmap 刪除後唯一 script）"
      pattern: "script type=\"module\" src=\"/index.tsx\""
    - from: "package.json dependencies"
      to: "Vite build"
      via: "npm ci 依 package-lock.json 安裝，Rollup 內聯五件套進 bundle"
      pattern: "react-markdown|recharts|lucide-react"
---

<objective>
D-1c（Phase D 3/7）：刪除 index.html 的休眠 esm.sh importmap（D-1a 已證實雙環境 0 請求＝行為中性純刪除），並將依賴管理文件單軌化——CLAUDE.md「依賴要同時維護兩處」關鍵事實與 STACK.md 的 importmap／CDN 敘述全數改為「僅 package.json＋lockfile」。

Purpose: 消除供應鏈死重（esm.sh URL 若被任何降級路徑啟用＝引入未經 lockfile 鎖定的第三方執行碼）、消除 `^` 範圍版本漂移風險、消除「維護兩處」的心智負擔（per optimization/PLAN.md D-1c 規格）。
Output: index.html（-12 行）、CLAUDE.md（1 行改寫）、STACK.md（4 處敘述更新）——1 個原子 commit。
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/optimization/PLAN.md（僅「### D-1c importmap 移除＋文件單軌化」章節，130-133 行）
@.planning/quick/260713-kq2-d-1a-importmap-tailwind-cdn-bundle-class/260713-kq2-SUMMARY.md（第 2 節：休眠死重判定；第 6 節 D-1c 行動建議）
@index.html
@CLAUDE.md
@.planning/codebase/STACK.md
</context>

<key_facts>
- **importmap 位置**：index.html 第 8-19 行——`<script type="importmap">` 開標籤到 `</script>` 閉標籤，內含 react-markdown／recharts／lucide-react／react-dom/／react/／react 六個 esm.sh 映射。這是唯一要刪的區塊。
- **一字不動清單**：第 7 行 Google Fonts link、第 22 行 `<div id="root">`、第 23 行 `<script type="module" src="/index.tsx">`、`<html lang="zh-TW">`、兩個 meta、title。index.css 掛載在 index.tsx（D-1b 放的），index.tsx 也不碰。
- **依賴基礎已齊**：package.json dependencies 已含五件套＋remark-gfm；package-lock.json 已在 D-1b 修復脫鉤（@upstash 兩件補登）。npm ci 應可直接過——它本身就是本包驗證點，不是前置負擔。
- **worktree 無 node_modules**：npm ci 是必經步驟。
- **executor 驗證上限**（無瀏覽器）：npm ci、npm run build、dist grep、tsc。「preview network 0 esm.sh 請求」與「App 全功能 smoke」由 orchestrator 合併後驗，不在本包。
- **STACK.md Tailwind 敘述**（line 52「CDN Play CDN」、line 88「cdn.tailwindcss.com」）是 D-1b 遺留的過時敘述——本包順手改正確（建置期 tailwindcss v3＋tailwind.config.js＋index.css），SUMMARY 記為本包偏差（planning context 已預授權）。
- **Windows＋Git Bash**：grep 驗證用 Bash 工具跑（PowerShell 5.1 沒有 grep）；改檔用 Write/Edit 工具。
</key_facts>

<tasks>

<task type="auto">
  <name>Task 1: 刪 index.html importmap 區塊＋npm ci／build 驗證</name>
  <files>index.html</files>
  <action>
用 Edit 工具刪除 index.html 第 8-19 行整塊（`<script type="importmap">` 至其 `</script>`，含六個 esm.sh 映射）。刪除後 head 僅剩：charset meta、viewport meta、title、Google Fonts link。其餘一字不動——特別是第 7 行 fonts link、body 內 root div 與 `/index.tsx` module script（per D-1c 規格與交辦紅線）。不碰 index.tsx、不碰 vite.config.ts。

改完後依序執行（Bash 工具、專案根目錄）：
1. `npm ci`——worktree 無 node_modules，此步同時驗證 package-lock.json 與 package.json 同步（D-1b 修復後首次乾淨驗證）。若失敗（EUSAGE lock 脫鉤），停下回報，不要改用 npm install 掩蓋。
2. `npm run build`——驗證 Vite/Rollup 在無 importmap 下從 node_modules 解析五件套＋remark-gfm 成功打包。

依據：D-1a SUMMARY 第 2 節——dev 端 Vite 預打包改寫裸模組、prod 端 Rollup 已內聯全部依賴，雙環境 esm.sh 請求 0，移除為行為中性純刪除，無需保留任何降級路徑。
  </action>
  <verify>
    <automated>npm ci 退出碼 0；npm run build 退出碼 0；`! grep -q "esm.sh" index.html`；`! grep -q "esm.sh" dist/index.html`（before 為 6 處）；`! grep -q "importmap" dist/index.html`；`! grep -rq "AIza" dist/`（金鑰紅線）；`grep -c 'script type="module" src="/index.tsx"' index.html` = 1（入口 script 完好）</automated>
  </verify>
  <done>index.html 無 importmap/esm.sh 字樣且 fonts link、root div、module script 原樣保留；npm ci＋npm run build 全綠；dist/index.html 0 esm.sh；dist 無 AIza。</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md＋STACK.md 單軌化＋tsc＋原子 commit</name>
  <files>CLAUDE.md, .planning/codebase/STACK.md</files>
  <action>
**CLAUDE.md（repo 根，line 54）**：把「本專案關鍵事實（易錯）」的「- 依賴要同時維護兩處：`package.json` 與 `index.html` 的 esm.sh importmap。」整行改寫為單軌敘述，語意須含：依賴只維護 `package.json`＋`package-lock.json` 一處；index.html 已無 importmap（Vite 從 node_modules 解析）。一行內完成（CLAUDE.md 有 150 行上限制度），例如：「- 依賴單軌：只維護 `package.json`＋`package-lock.json`（index.html 的 esm.sh importmap 已移除，Vite 從 node_modules 解析）。」其他行不動。

**.planning/codebase/STACK.md**（四處定點更新，其餘不動）：
1. **line 20**（Runtime > Browser runtime）：刪除「native ES module and import-map support…loads its npm dependencies from a CDN import map (esm.sh)」敘述，改為：modern ES2022 browser；所有 npm 依賴由 Vite 打包進 bundle，不依賴 CDN 載入模組。
2. **line 51-52**（Styling (not in package.json)）：D-1b 遺留過時敘述——改為建置期 Tailwind。先用 Read 確認 package.json devDependencies 的 tailwindcss/postcss/autoprefixer 實際版本與 tailwind.config.js／index.css 存在，再改寫為：Tailwind CSS v3（build-time，tailwind.config.js＋index.css，經 PostCSS 產出靜態 CSS）；小節標題「(not in package.json)」同步改掉（現在就在 package.json）。Google Fonts 照舊保留敘述（D-1e 既定決策：保留 CDN）。
3. **line 70**（Configuration > index.html）：刪除 importmap 描述（注意該行還誤列了 @google/genai——現行 importmap 根本沒有它，一併消失），改為：入口文件僅含 meta/title/Google Fonts link/root div/index.tsx module script；順手刪掉「References /index.css (no such file…)」的過時猜測（index.css 現由 index.tsx import）。
4. **line 88**（Production）：第三方 CDN 依賴清單移除 esm.sh 與 cdn.tailwindcss.com，改為僅剩 Google Fonts 與 public CORS proxies（行情鏈）。
5. 文末註記行補一條：`*Updated 2026-07-13: D-1b build-time Tailwind + D-1c importmap removal（依賴單軌化）*`。

改完後 STACK.md 全檔不得再出現 esm.sh 或 cdn.tailwindcss.com 字樣（歷史脈絡留給 SUMMARY，不留在現況文件）。

**驗證後 commit（1 個原子 commit）**：
1. `npx tsc --noEmit`（最低驗證制度；本包雖沒改 .ts，但 npm ci 後首次可跑，順帶確認依賴齊全）。
2. `git add index.html CLAUDE.md ".planning/codebase/STACK.md"` 後 commit，訊息：`refactor(d-1c): 移除休眠 esm.sh importmap，依賴與文件單軌化（package.json＋lockfile）`。**SUMMARY 不入此 commit**（orchestrator 統一收尾），除上列三檔外不得 add 其他檔案（node_modules、dist、package-lock.json 均不應有 diff——若 npm ci 意外改動 lockfile，停下回報而非提交）。
  </action>
  <verify>
    <automated>`npx tsc --noEmit` 退出碼 0；`! grep -q "依賴要同時維護兩處" CLAUDE.md`；`grep -q "package-lock.json" CLAUDE.md`；`! grep -qi "esm.sh" .planning/codebase/STACK.md`；`! grep -q "cdn.tailwindcss.com" .planning/codebase/STACK.md`；`git show --stat HEAD` 恰含 index.html、CLAUDE.md、.planning/codebase/STACK.md 三檔</automated>
  </verify>
  <done>兩份文件無殘留雙軌／importmap／Tailwind CDN 敘述；tsc 綠；HEAD 為單一原子 commit 且只含三檔。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→esm.sh CDN | 本包移除此邊界——importmap 死重刪除後，瀏覽器不再有任何載入 esm.sh 執行碼的潛在路徑 |
| build→npm registry | npm ci 依既有 package-lock.json 安裝，無新增套件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D1c-01 | Tampering | index.html importmap（esm.sh 未鎖版執行碼） | mitigate | 本包主體：整塊刪除，供應鏈面縮減；驗收 `! grep esm.sh`（index.html＋dist） |
| T-D1c-02 | Information Disclosure | dist/ bundle（金鑰洩漏） | mitigate | 每次 build 後 `grep -r "AIza" dist/` 必須無結果（Task 1 verify 內建） |
| T-D1c-SC | Tampering | npm ci 安裝 | accept | 零新套件，僅照 lockfile 還原既有依賴；lockfile 若被 npm ci 改動即停下回報 |
</threat_model>

<verification>
1. `npm ci` 成功（lockfile 同步驗證，D-1b 修復後首次乾淨驗證）。
2. `npm run build` 成功；`dist/index.html` grep esm.sh = 0（before 6 處）、grep importmap = 0。
3. `grep -r "AIza" dist/` 無結果。
4. `npx tsc --noEmit` 綠。
5. CLAUDE.md 無「依賴要同時維護兩處」；STACK.md 全檔無 esm.sh／cdn.tailwindcss.com。
6. index.html 僅少 importmap 12 行，fonts link／root div／module script 原樣（git diff 目視確認只有一個刪除 hunk）。
7. （orchestrator 合併後）preview network 0 esm.sh 請求＋App 全功能 smoke——不在 executor 範圍。
</verification>

<success_criteria>
- D-1c 規格三項全落地：importmap 刪除、npm ci 後 build 正常、CLAUDE.md＋STACK.md 單軌敘述。
- 1 個原子 commit（index.html＋CLAUDE.md＋STACK.md），可單獨回滾；SUMMARY 未入 commit。
- 偏差記錄：STACK.md Tailwind CDN 敘述順手改正（D-1b 遺留，planning context 預授權）寫入 SUMMARY Deviations。
</success_criteria>

<output>
Create `.planning/quick/260713-mi1-d-1c-importmap-index-html-esm-sh-importm/260713-mi1-SUMMARY.md` when done（不 commit——orchestrator 統一收尾）。
</output>
