---
phase: quick-260712-0dq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-README]
must_haves:
  truths:
    - "README 開頭介紹本專案：台股／美股技術分析工具，朱家泓六六大順進場法則＋Gemini AI 中文分析"
    - "README 有 Run Locally 節，涵蓋 npm install、GEMINI_API_KEY（Vercel 環境變數／.env.local）、本地雙伺服器（vercel dev 3001 + Vite 3000）"
    - "README 有一節逐一記錄 .claude/skills 內的 10 個 skill（7 步驟朱家泓進場法 + tw-fundamentals + phase-loop + start-dev）"
    - "原 AI Studio 預設模板文字（含 GHBanner 橫幅、ai.studio 連結、Run and deploy your AI Studio app）已全數移除"
  artifacts:
    - path: "README.md"
      provides: "專案介紹＋在地執行說明＋10 個 Claude skill 說明（繁體中文）"
      contains: "entry-decision"
  key_links:
    - from: "README.md"
      to: ".claude/skills/"
      via: "skills 說明節引用 skill 目錄名稱"
      pattern: "\\.claude/skills"
---

<objective>
用 `.claude/skills/` 內 10 個 SKILL.md 的實際內容，重寫 `README.md`，取代目前的 AI Studio 預設模板。
新 README 為繁體中文，涵蓋三大區塊：(1) 專案介紹、(2) 在地執行說明、(3) 10 個 Claude skill 說明。

Purpose: 目前 README 是 AI Studio 自動產生的空模板，對本專案毫無資訊價值；讓 repo 有一份能反映「朱家泓六六大順進場法 + Gemini AI 分析 + 前端 SPA/Vercel 代理」與 10 個 skill 的正式說明文件。
Output: 全新內容的 `README.md`（繁中）。
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@README.md
@.claude/skills/trend-analysis/SKILL.md
@.claude/skills/position-analysis/SKILL.md
@.claude/skills/kline-signal/SKILL.md
@.claude/skills/ma-structure/SKILL.md
@.claude/skills/volume-analysis/SKILL.md
@.claude/skills/indicator-analysis/SKILL.md
@.claude/skills/entry-decision/SKILL.md
@.claude/skills/tw-fundamentals/SKILL.md
@.claude/skills/phase-loop/SKILL.md
@.claude/skills/start-dev/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重寫 README.md（專案介紹＋在地執行＋10 個 skill 說明）</name>
  <files>README.md</files>
  <action>
用 Write 工具「整檔覆寫」`README.md`（不是局部編輯），移除全部 AI Studio 模板內容（第 1-3 行 GHBanner `<div>`／`<img>`、`# Run and deploy your AI Studio app`、`View your app in AI Studio: https://ai.studio/...` 連結）。全文用繁體中文。

**只根據已載入 context 的 CLAUDE.md 與 10 份 SKILL.md 撰寫，不要臆造任何未出現的事實**（尤其不要編造未證實的效能數字、部署 URL、授權條款、螢幕截圖）。

新 README 需含下列區塊，順序如下：

1. **標題與簡介**：標題 `# Taiwan & USA Stock AI Analyst`。一段介紹（取自 CLAUDE.md「## Project」）：給個人投資者的台股／美股技術分析工具（繁中介面）；搜尋股票 → 抓行情、算技術指標 → 依朱家泓「六六大順」法則產出客觀 GO／WAIT／NO_GO → Google Gemini 產生中文分析報告；另有可做 AI 健檢的庫存（Portfolio）功能。點明目前為純前端 React SPA，外部呼叫改由 Vercel Serverless 函式代理（`GEMINI_API_KEY` 只存在 Vercel 環境變數，絕不進前端 bundle 或 git）。

2. **核心價值 / 特色**（條列，取自 CLAUDE.md Core Value 與 Constraints）：客觀進場判斷＋AI 中文解讀；行情來源 Yahoo Finance（非官方）＋ FinMind 免費層；金鑰後端代理保護；力求落在 Vercel 免費層。

3. **## 在地執行（Run Locally）**：改寫現有簡略說明，需涵蓋：
   - 前置：Node.js。
   - `npm install`。
   - 環境變數：`GEMINI_API_KEY` 正式部署放 Vercel 環境變數；本地開發放 `.env.local`／`.env`（依 start-dev skill：另有 `GEMINI_MODEL_FAST`、`GEMINI_MODEL_THINKING`、`ALLOWED_ORIGIN`、選填 `FINMIND_TOKEN`）。強調金鑰不可進前端 bundle 或 git。
   - 本地雙伺服器（取自 start-dev SKILL.md）：後端 `npx vercel dev --listen 3001` + 前端 `npm run dev`（Vite 3000）；瀏覽器開 http://localhost:3000，`/api` 由 Vite proxy 轉給 3001。可提及 `start-dev` skill 為固定起環境流程。
   - 金鑰驗證（取自 CLAUDE.md）：`npm run build` 後對 `dist/` 掃 `AIza` 應無結果。

4. **## Claude Code Skills（`.claude/skills/`）**：一段前言說明這些 skill 讓 Claude／Codex 依朱家泓法則做技術分析與開發工作流；Codex 讀鏡像 `.agents/skills/`（見 CLAUDE.md）。接著分三小節：

   4a. **朱家泓 7 步驟進場分析 pipeline**（依序 `trend-analysis` → `position-analysis` → `kline-signal` → `ma-structure` → `volume-analysis` → `indicator-analysis` → `entry-decision`），建議用有序清單或表格，每列一行說明（依 SKILL.md 內容，勿臆造）：
      - 步驟1 `trend-analysis` 趨勢研判：判定多頭（頭頭高底底高）／空頭／盤整，比對日線與週線；盤整退出觀察。
      - 步驟2 `position-analysis` 當下位置：定位打底／初升／主升／末升段＋起漲／回檔／高檔，套回檔 1/2 法則評估追高風險。
      - 步驟3 `kline-signal` K線轉折：判讀單一／組合K棒，檢查多頭關鍵進場K線（價漲>2%、攻擊量、突破5均並過前一日高）。
      - 步驟4 `ma-structure` 均線架構：檢查至少 3 線多排、方向向上、股價站上月線 MA20、均線糾結突破。
      - 步驟5 `volume-analysis` 量價關係：價漲量增／攻擊量（雙軌擇一：>昨量1.3倍 或 >5日均量1.2倍）、辨識出貨量與量價背離。
      - 步驟6 `indicator-analysis` 指標：KD 黃金交叉多排（高檔鈍化回歸價量）、MACD 紅柱延長／背離、缺口與底部型態。
      - 步驟7 `entry-decision` 進場決策彙總：彙總前 6 步，比對選股 SOP 6 項＋六大買點＋做多 10 大戒律，輸出 GO／等待／NO-GO＋建議進場價、停損雙軌、停利規則、信心評分（整套分析的總入口與最終結論）。
      並註明使用方式（取自 CLAUDE.md）：使用者說「分析 XXXX」「XXXX 能不能買」時從 `trend-analysis` 依序跑，或直接用 `entry-decision` 帶完整流程；各步驟細節讀該 skill 的 SKILL.md。

   4b. **台股基本面資料層**：`tw-fundamentals` — 用 FinMind 免 token 抓台股損益表／資產負債表／現金流量表／PER-PBR-殖利率／月營收（YoY）／股利，整備後餵給美股估值 skill（dcf-model／comps-analysis／initiating-coverage／earnings-analysis）；補上台股缺、SEC 查不到的財報層。抓取腳本 `.claude/skills/_shared/fetch_fundamentals.py`。

   4c. **工作流 skill**：
      - `phase-loop` — 三角開發迴圈 playbook：規劃 → 交 Codex 執行 → Opus/Sonnet 覆核 → 人工驗證 → 合併回 main 四階段。
      - `start-dev` — 起開發環境固定流程：助手用 PowerShell Start-Process 開兩視窗（後端 vercel dev 3001＋前端 Vite 3000）並輪詢就緒。

可視情況補一段簡短「技術棧／資料鏈」說明（取自 CLAUDE.md：依賴同時維護 package.json 與 index.html importmap；Yahoo→FinMind fallback），但保持精簡，不要展開成大量文件。全文以清楚、資訊密度高、可直接閱讀為準。
  </action>
  <verify>
    <automated>bash -c "test -f README.md && echo EXISTS"</automated>
  </verify>
  <done>README.md 已整檔覆寫為繁中內容，含專案介紹、在地執行、10 個 skill 說明，無 AI Studio 模板殘留。</done>
</task>

<task type="auto">
  <name>Task 2: 內容檢查驗證（10 skill 名稱齊全、無 AI Studio 殘留、有執行說明）</name>
  <files>README.md</files>
  <action>
用 Bash 工具（本專案 PowerShell 5.1 無 grep，用 Bash 工具跑）做純內容檢查，無 test runner／非 TypeScript，故不需 tsc。逐項確認並在 SUMMARY 記錄結果：

1. 10 個 skill 目錄名稱皆出現於 README（去重後應為 10）。
2. 無 AI Studio 模板殘留字串（`ai studio`、`ai.studio`、`GHBanner` 皆為 0 次）。
3. 有在地執行關鍵字（`npm install`、`vercel dev`、`GEMINI_API_KEY`）。

若任一項不符，回 Task 1 修正 README 後再驗，直到全數通過。
  </action>
  <verify>
    <automated>bash -c "n=$(grep -Eo 'trend-analysis|position-analysis|kline-signal|ma-structure|volume-analysis|indicator-analysis|entry-decision|tw-fundamentals|phase-loop|start-dev' README.md | sort -u | wc -l); echo skills=$n; a=$(grep -ci 'ai\\.studio\\|ai studio\\|GHBanner' README.md); echo aistudio=$a; r=$(grep -c 'npm install' README.md); echo runlocal=$r; [ \"$n\" -eq 10 ] && [ \"$a\" -eq 0 ] && [ \"$r\" -ge 1 ] && echo PASS || echo FAIL"</automated>
  </verify>
  <done>驗證輸出 `skills=10`、`aistudio=0`、`runlocal>=1` 且結尾為 `PASS`。</done>
</task>

</tasks>

<verification>
- README.md 存在且為繁中重寫版本。
- 10 個 skill 名稱（trend-analysis、position-analysis、kline-signal、ma-structure、volume-analysis、indicator-analysis、entry-decision、tw-fundamentals、phase-loop、start-dev）皆出現。
- 無 AI Studio 模板殘留（ai.studio 連結、GHBanner、Run and deploy your AI Studio app）。
- 在地執行說明含 npm install、vercel dev 3001 + Vite 3000、GEMINI_API_KEY 環境變數。
- 內容全部有 SKILL.md／CLAUDE.md 依據，無臆造事實。
（本專案無 test runner／lint，此為文件檔非 TypeScript，驗證僅做內容檢查，不需 tsc。）
</verification>

<success_criteria>
README.md 已取代 AI Studio 預設模板，成為一份繁中專案說明：涵蓋專案介紹、在地執行步驟、與 `.claude/skills/` 內 10 個 skill 的說明（7 步驟朱家泓進場法 + tw-fundamentals + phase-loop + start-dev），且所有描述均源自實際 SKILL.md 與 CLAUDE.md。
</success_criteria>

<output>
Create `.planning/quick/260712-0dq-claude-skills-10-skill-md-readme-md/260712-0dq-SUMMARY.md` when done
</output>
