---
phase: quick-260602-0u1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/yahoo.ts
autonomous: true
requirements:
  - BUGFIX-yahoo-latest-null-close
must_haves:
  truths:
    - "當最新一根日線 K 棒 close=null 但 meta.regularMarketPrice 為有限數 >0 時，序列最後一根的日期為 regularMarketTime 對應的當日（如 2026-06-01），而非退回前一個完整交易日（如 2026-05-29）"
    - "合成的最新 K 棒 open/high/low/close 皆等於 regularMarketPrice，volume 為 0（若 FinMind 該日有真實量則由既有 override 取代）"
    - "中間零星 null K 棒維持既有丟棄行為，不被合成補值"
    - "週線/月線/分鐘線（1wk/1mo/60m/15m）行為完全不變，補值僅在 interval==='1d' 套用"
    - "合成 K 棒的 RAW 與 ADJUSTED 指標欄位皆有值、無 NaN"
  artifacts:
    - path: "services/yahoo.ts"
      provides: "processYahooResult 對最新一根 null-close 日線 K 棒以 regularMarketPrice 合成補值；YahooChartResponse.meta 新增 regularMarketTime 欄位"
      contains: "regularMarketTime"
  key_links:
    - from: "services/yahoo.ts:processYahooResult"
      to: "response.chart.result[0].meta.regularMarketPrice / regularMarketTime"
      via: "讀取 meta 合成最新一根 K 棒並 push 至 cleanData 尾端"
      pattern: "meta\\.regularMarketPrice"
    - from: "services/yahoo.ts:getStockData"
      to: "processYahooResult"
      via: "既有呼叫，簽章不變（meta 由 response 取得，無需改介面）"
      pattern: "processYahooResult\\(mainResponse"
---

<objective>
修正 `services/yahoo.ts` 的 `processYahooResult`：當 Yahoo 回傳的最新一根日線 K 棒 `close` 為 `null`（盤後 Yahoo 尚未把當日收盤填入歷史 quote 陣列），但 `meta.regularMarketPrice` 為有效值時，用 `regularMarketPrice` 合成補上這最後一根 K 棒，讓儀表板顯示最新交易日而非退回前一個完整交易日。

Purpose: 修掉「dashboard 退回顯示前一交易日」的資料層 bug（已查證：2330.TW 6/01 那根 close=null、meta.regularMarketPrice=2355、regularMarketTime=2026-06-01 13:30，目前被整根丟棄退回 5/29）。

Output: `processYahooResult` 在 1d 情境下對「序列最新一根被 null 過濾掉的 K 棒」合成補值；`YahooChartResponse.meta` 介面新增 `regularMarketTime` 欄位。`StockDataPoint` 契約零變動。
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@services/yahoo.ts
@types.ts
</context>

<interface_context>
合成 K 棒必須與 `processYahooResult` 既有 push 物件 shape 完全一致，否則下游（FinMind 量能 override 第 632-633 行、法人籌碼 merge 第 636 行、指標 map 第 645-713 行）會讀到 undefined：

既有 cleanData 物件欄位（services/yahoo.ts:400-416）：
- date (string, formatExchangeDate 產出)
- timestamp (number, 秒)
- rawHour, rawMinute (number, getExchangeTime 產出)
- rawDateStr (string YYYY-MM-DD，下游 volume/chip merge 的 key)
- open, high, low, close (number)
- volume (number)
- openAdj, highAdj, lowAdj, closeAdj (number)
- exchangeTimezone (string)

關鍵下游依賴：
- 第 632 行 `volumeMap.has(d.rawDateStr || d.date)` → 合成 K 棒必須帶 rawDateStr，FinMind 若有該日真實量會自動覆蓋 volume:0（這是可接受且更正確的行為）。
- 第 636 行 chip merge 同樣 key 在 rawDateStr。
- 第 645-648 行指標把整個陣列依 index 取 close/high/low/closeAdj；合成 K 棒接在尾端 index = length-1，open/high/low/close 全為 regularMarketPrice（有限數）→ 指標無 NaN。
- adjClose：盤中無調整資訊，ratio=1，closeAdj = regularMarketPrice。

meta 取值（YahooChartResponse.meta，介面約 16-44 行）：
- `regularMarketPrice: number`（已存在）
- `regularMarketTime`（**目前介面缺，需新增** `regularMarketTime?: number`，Yahoo 回傳為 Unix 秒）
</interface_context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 擴充 YahooChartResponse.meta 介面並抽出最新-null-close 合成補值邏輯</name>
  <files>services/yahoo.ts</files>
  <behavior>
    - YahooChartResponse.meta 介面新增 `regularMarketTime?: number`（Unix 秒，與 regularMarketPrice 同層）。
    - processYahooResult 在既有 forEach（約 391-418 行）跑完、cleanData 組好後，新增「最新一根補值」判定區塊（在 `return cleanData;` 之前）。
    - 補值「僅在 interval === '1d'」時執行；其他 interval（1wk/1mo/60m/15m）一律略過，直接 return cleanData。
  </behavior>
  <action>
    在 YahooChartResponse 的 meta 物件型別中，於既有 `regularMarketPrice: number;` 旁新增 `regularMarketTime?: number;`（Yahoo 回傳的當日收盤/最新報價 Unix 秒時間戳，目前介面未宣告）。

    在 processYahooResult 內、`return cleanData;` 之前、且僅當 `interval === '1d'` 時，加入「最新一根 null-close 合成補值」邏輯：

    1. 取得序列中時間最新的原始 K 棒 index：令 `lastIdx = timestamps.length - 1`。只處理「序列最後一根」這一根；中間零星 null 維持既有丟棄行為，不要掃描或補任何中間根。
    2. 判定這最後一根是否「被既有 forEach 過濾掉」：條件為 `closes[lastIdx] === null`（即原本因 close=null 沒進 cleanData）。若該根本來就有效（close 非 null，已在 cleanData），則不需補值，跳過。
    3. 判定 regularMarketPrice 是否可用：令 `rmp = meta.regularMarketPrice`，需 `Number.isFinite(rmp) && rmp > 0`。不可用則跳過（維持現狀）。
    4. 防重複：若 cleanData 最後一筆的 timestamp 已等於該根時間來源所對應的時間戳，則不重複 push。
    5. 決定合成 K 棒的時間戳 `synthTs`：優先用 `meta.regularMarketTime`（若為有限數 >0），否則退回 `timestamps[lastIdx]`。用 `synthTs` 經既有 `getExchangeTime(synthTs, meta.exchangeTimezoneName, isTaiwanStock)` 取得 rawHour/rawMinute/rawDateStr，經既有 `formatExchangeDate(synthTs, meta.exchangeTimezoneName, '1d')` 取得 date —— 與其他 K 棒一致的 exchangeTimezone 邏輯，禁止自行用 new Date 手算時區。
    6. 合成並 push 至 cleanData 尾端，欄位與既有 push 物件（約 400-416 行）完全同名同 shape：open/high/low/close 皆為 `rmp`；volume 為 0（避免污染攻擊量等量能判斷）；openAdj/highAdj/lowAdj/closeAdj 皆為 `rmp`（盤中無調整資訊，ratio=1）；timestamp 為 `synthTs`；rawHour/rawMinute/rawDateStr/exchangeTimezone 帶入步驟 5 的值。

    註記在程式碼註解：volume=0 僅為避免量能污染；TW 1d 若 FinMind 該日已有真實 Trading_Volume，既有第 632-633 行 override 會自動以真實量取代（這是預期且更正確的行為，不需特別處理）。

    不要改 getStockData 簽章、不要改 processYahooResult 的參數列（meta 由 response 內取得即可）。不要動 getLatestPrice、其他 service、vite.config.ts、金鑰邏輯。不要新增依賴。
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('services/yahoo.ts','utf8'); const i=r.indexOf('regularMarketPrice: number'); if(r.indexOf('regularMarketTime')<0) throw new Error('regularMarketTime not added'); if(!/interval === '1d'/.test(r.slice(r.indexOf('processYahooResult'), r.indexOf('export const getLatestPrice')))) throw new Error('1d guard missing in processYahooResult'); if(!/Number\.isFinite\([^)]*[rR]egularMarketPrice|Number\.isFinite\(\s*rmp/.test(r)) throw new Error('finite guard on regularMarketPrice missing'); console.log('static checks passed');"</automated>
  </verify>
  <done>YahooChartResponse.meta 含 regularMarketTime?: number；processYahooResult 內有 interval==='1d' 守衛的最新-null-close 合成補值區塊，使用 Number.isFinite 守衛 regularMarketPrice、用 getExchangeTime/formatExchangeDate 換算時間、push 物件 shape 與既有 cleanData 一致、volume=0。getStockData 簽章與其他檔案未被更動。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TypeScript 型別檢查與序列尾端不變式驗證</name>
  <files>services/yahoo.ts</files>
  <behavior>
    - 整個專案 tsc --noEmit 通過（不引入新型別錯誤；專案 strict 未開，至少不可有阻斷性錯誤）。
    - 確認補值區塊不會在「最後一根本來就有效」「regularMarketPrice 不可用」「非 1d」三種情況下改變輸出。
  </behavior>
  <action>
    執行 TypeScript 型別檢查（專案無 typecheck script，直接呼叫 tsc）：`npx tsc --noEmit`。確認沒有因新增 regularMarketTime 欄位或補值邏輯產生的型別錯誤。專案 strict 未開啟、既有可能存在 implicit any，重點是不可引入「新的、阻斷性的」型別錯誤；若 tsc 既有就有與本次無關的錯誤，比對改動前後差異即可，不需順手修無關錯誤。

    人工/邏輯複查補值守衛三條不變式（在 action 內以程式邏輯確認，非新增測試檔）：
    1. 當 `closes[lastIdx] !== null`（最後一根本來有效）→ 不 push 任何合成根（cleanData 長度與行為等同未改前）。
    2. 當 `meta.regularMarketPrice` 非有限數或 ≤0 → 不 push（退回既有行為，序列尾端為前一完整交易日）。
    3. 當 `interval !== '1d'` → 整段補值略過。

    不新增測試框架（專案目前無測試 runner，constraint 為純小修不引入依賴）。驗證以 tsc 與靜態掃描為主。
  </action>
  <verify>
    <automated>npx tsc --noEmit; if ($LASTEXITCODE -ne 0) { Write-Error 'tsc reported errors'; exit 1 } else { Write-Output 'tsc --noEmit passed' }</automated>
  </verify>
  <done>npx tsc --noEmit 對 services/yahoo.ts 的改動無阻斷性型別錯誤；補值三條守衛（最後一根有效 / regularMarketPrice 不可用 / 非 1d）在程式邏輯上確認不改變既有輸出。</done>
</task>

</tasks>

<verification>
- 介面：`YahooChartResponse.meta` 含 `regularMarketTime?: number`。
- 邏輯：`processYahooResult` 在 `interval === '1d'` 且最後一根 `closes[lastIdx]===null` 且 `Number.isFinite(regularMarketPrice) && >0` 時，於 cleanData 尾端 push 一根 open=high=low=close=closeAdj=regularMarketPrice、volume=0 的合成 K 棒，時間用 getExchangeTime/formatExchangeDate 由 regularMarketTime（退回 timestamps[lastIdx]）換算。
- 不變式：最後一根本來有效 / regularMarketPrice 不可用 / 非 1d → 不補值。
- 相容：StockDataPoint 契約零變動；getStockData 簽章不變；getLatestPrice、其他 service、vite.config.ts、金鑰邏輯未動。
- 型別：`npx tsc --noEmit` 無新增阻斷性錯誤。
</verification>

<success_criteria>
- 對最新一根日線 close=null 但 regularMarketPrice 有效的個股（如查證的 2330.TW 6/01 案例），dashboard 序列最後一根日期為當日（2026-06-01）而非退回前一交易日（2026-05-29），其 OHLC=regularMarketPrice、volume=0（或 FinMind 真實量）。
- 週/月/分鐘線及「中間零星 null」情境行為不變。
- 合成 K 棒的 RAW/ADJUSTED 指標欄位皆有值、無 NaN。
- TypeScript 型別檢查通過。
</success_criteria>

<output>
Create `.planning/quick/260602-0u1-services-yahoo-ts-k-close-null-regularma/260602-0u1-SUMMARY.md` when done
</output>
