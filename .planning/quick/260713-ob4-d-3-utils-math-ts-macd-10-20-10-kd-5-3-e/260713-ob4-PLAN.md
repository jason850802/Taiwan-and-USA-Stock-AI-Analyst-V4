---
phase: quick-260713-ob4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - utils/math.test.ts
  - utils/entryFilter.test.ts
autonomous: true
requirements: [D-3]

must_haves:
  truths:
    - "npm run test 一鍵執行且全綠（vitest run）"
    - "math.ts 每個 export 函式有 >=2-3 個斷言案例，且 MACD 10,20,10 與 KD period=5 的非標準參數行為被明確鎖住"
    - "runEntryFilter 有 GO / WAIT / NO_GO 三種決策的合成輸入斷言"
    - "npx tsc --noEmit 綠、npm run build 綠、dist/ 無 AIza 也無 vitest/test 內容"
    - "utils/math.ts 與 utils/entryFilter.ts 本體零改動"
  artifacts:
    - path: "utils/math.test.ts"
      provides: "SMA/EMA/RSI/MACD/Bollinger/KDJ 解析案例＋黃金值回歸鎖"
      min_lines: 80
    - path: "utils/entryFilter.test.ts"
      provides: "runEntryFilter GO/WAIT/NO_GO 決策斷言＋停損雙軌欄位斷言"
      min_lines: 60
    - path: "package.json"
      provides: "test script 與 vitest devDependency"
      contains: "\"test\": \"vitest run\""
  key_links:
    - from: "utils/math.test.ts"
      to: "utils/math.ts"
      via: "relative import"
      pattern: "from ['\"]\\./math['\"]"
    - from: "utils/entryFilter.test.ts"
      to: "utils/entryFilter.ts"
      via: "relative import"
      pattern: "from ['\"]\\./entryFilter['\"]"
    - from: "package.json"
      to: "vitest"
      via: "npm script"
      pattern: "vitest run"
---

<objective>
D-3 最小單元測試（Phase D 6/7）：為 `utils/math.ts`（非標準參數 MACD 10,20,10／KD 5,3）與 `utils/entryFilter.ts` 建立本專案第一條測試跑道。選 vitest（規格傾向＋與 vite@6 原生整合、TS 零設定、tsconfig 零改動），`npm run test` 一鍵全綠。

Purpose: 鎖住指標計算與六六大順決策的現行行為，讓後續 Phase D 剩餘項目與 BL backlog 改碼時有回歸防線。本包是「鎖行為」不是「改行為」。

Output: `utils/math.test.ts`、`utils/entryFilter.test.ts`、package.json test script＋vitest devDependency、更新的 package-lock.json。一個原子 commit。
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/optimization/PLAN.md （只讀「### D-3 最小單元測試」一節）
@utils/math.ts
@utils/entryFilter.ts
@package.json
@tsconfig.json
</context>

<constraints>
- **紅線：不改 `utils/math.ts` 與 `utils/entryFilter.ts` 本體。** 純加測試。若測試過程發現疑似 bug（例：`calculateRSI` 對常數序列在 index=period 產生 NaN、之後因 avgLoss===0 變 100；`calculateRSI` 把 diff===0 計入 gain 側），記入 SUMMARY「發現但不修」清單，斷言繞開這些輸入或以黃金值鎖現行行為，不得改實作。
- **tsconfig 零改動。** 測試檔一律顯式 `import { describe, it, expect } from 'vitest'`，不用 globals 模式，因此 tsconfig（無 include/exclude，`types: ["node"]`）不需加 vitest types 也能過 `npx tsc --noEmit`。
- **不碰 index.html importmap。** vitest 是 devDependency（測試期），不是 runtime 依賴，「package.json＋importmap 兩處同步」規則不適用。
- **不需要 vitest.config。** vitest 會讀現有 vite.config.ts（無不相容項：plugin-react、alias、manualChunks 皆無害）；預設 include 涵蓋 `utils/*.test.ts`，預設 environment=node 對純函式正確。除非實跑失敗才允許新增最小 vitest.config.ts（若新增，一併納入 commit）。
- **測試策略雙層，SUMMARY 必須誠實區分：**
  (a) 解析案例＝手算可驗的已知輸入輸出（常數／線性序列的可推導性質、合成 K 棒的明確決策）；
  (b) 黃金值回歸鎖＝用「現行實作」對固定種子序列跑出的輸出寫死進 toBeCloseTo——只鎖行為防改壞，不證明數學正確性。SUMMARY 不得把 (b) 說成 (a)。
- **單一原子 commit**：Task 1、2 不 commit，Task 3 結尾一次 commit 全部產物。SUMMARY 不 commit。
- worktree 無 node_modules：先 `npm ci` 再裝 vitest。Windows 環境，驗證指令用 Bash 工具（PowerShell 5.1 無 grep／&&）。
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: 安裝 vitest（鎖 minor）＋ 加 test script</name>
  <files>package.json, package-lock.json</files>
  <action>
    在 repo 根目錄（Bash 工具）依序執行：
    1. `npm ci`（worktree 無 node_modules）。
    2. 供應鏈驗證（T-ob4-SC 緩解）：`npm view vitest name repository.url dist-tags.latest` 確認套件名精確為 `vitest`、repository 指向 github.com/vitest-dev/vitest（vitest 為 Vite 官方團隊維護的主流測試框架，屬 [OK] 級，不需人工 checkpoint）。
    3. 版本相容性：`npm view vitest@3 version --json` 取得最新 3.x；`npm view vitest@<該版> peerDependencies` 確認 peer range 含 vite ^6（vitest 3.2.x 的 peer 為 vite ^5/^6/^7，本專案 vite@^6.2.0 相容）。即使已有 vitest 4.x，仍鎖 3.x（key fact 指定）。
    4. `npm i -D vitest@~<最新3.x>`（tilde 鎖 minor 免驚喜）。
    5. 用 Edit 工具在 package.json scripts 加 `"test": "vitest run"`（放 preview 之後、sync:skills 之前，維持現有排版）。
    不 commit（原子 commit 留到 Task 3）。
  </action>
  <verify>
    <automated>npx vitest --version 輸出 3.x 版號；node -e "const p=require('./package.json'); if(p.scripts.test!=='vitest run'||!p.devDependencies.vitest) process.exit(1)"</automated>
  </verify>
  <done>vitest 3.x（~ 鎖 minor）進 devDependencies、package-lock.json 更新、`npm run test` script 存在（此時跑會因無測試檔失敗屬預期，vitest 「no test files found」預設 exit 1）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: utils/math.test.ts — 六個 export 的解析案例＋MACD/KDJ 黃金值鎖</name>
  <files>utils/math.test.ts</files>
  <behavior>
    測試檔開頭顯式 `import { describe, it, expect } from 'vitest'`，從 `./math` import 全部六個 export。每函式 >=2-3 案例：

    **(a) 解析案例（手算可驗）：**
    - calculateSMA：(1) data 長度小於 period 回傳全 null；(2) 常數序列 [5,5,5,5,5] period 3 → [null,null,5,5,5]；(3) 線性 [1..6] period 3 → [null,null,2,3,4,5]。
    - calculateEMA：(1) 長度不足全 null；(2) 常數序列自 index period-1 起恆等於該常數；(3) [1,2,3,4,5] period 3 → [null,null,2,3,4]（SMA 種子 2、k=0.5 手算）。
    - calculateRSI：(1) 長度 < period+1 全 null；(2) 嚴格遞增序列（如 1..17，period 14）自 index 14 起為 100（avgLoss=0 分支）；(3) 嚴格遞減序列自 index 14 起為 0。注意：常數序列會踩 NaN/100 怪行為，解析層繞開不測，如要鎖用黃金值並在 SUMMARY 記為疑似 bug。
    - calculateMACD（重點——鎖 10,20,10）：(1) 常數序列（長度 >=40）：macdLine 自 index 19 起 ~0、signalLine 自 index 28 起 ~0、histogram 兩者皆有值處 ~0（toBeCloseTo 0, 10）；(2) **warm-up 邊界鎖參數**：macdLine[18]===null 且 macdLine[19]!==null（編碼 slow=20）、signalLine[27]===null 且 signalLine[28]!==null（編碼 validStart 19＋signal 10）；(3) 線性遞增序列 warm-up 後 macdLine 每一項 >0（快線減慢線對上升趨勢恆正的解析性質）；(4) **預設參數即 10,20,10**：calculateMACD(series) 與 calculateMACD(series,10,20,10) 輸出逐項相等，且與 calculateMACD(series,12,26,9) 在 warm-up 後至少一個 index 不同。
    - calculateBollingerBands：(1) 長度不足全 null；(2) 常數序列 upper===middle===lower===常數；(3) [1,2,3,4] period 3 multiplier 2 → middle[2]=2、upper[2]=2+2*sqrt(2/3)（toBeCloseTo, 5）。
    - calculateKDJ（重點——鎖 period=5＋1/3,2/3 平滑）：(1) 常數序列（maxHigh===minLow → rsv=50）K/D/J 全程 50；(2) **warm-up 鎖參數**：對變動序列 index 0..3 的 K/D/J 維持初始值 50（loop 自 period-1=4 起，編碼 period=5）；(3) 遞增序列（highs=lows=closes）手算：K[4]=(2/3)*50+(1/3)*100=200/3、D[4]=(2/3)*50+(1/3)*(200/3)=500/9、J[4]=3K-2D（toBeCloseTo, 4），且 K 序列單調遞增趨向 100、K/D 全程落在 [0,100]；(4) **預設 period 即 5**：calculateKDJ(h,l,c) 與 calculateKDJ(h,l,c,5) 相等、與 period 9 對變動序列不同。

    **(b) 黃金值回歸鎖（獨立 describe 區塊，註解標明「golden regression lock — 值取自現行實作，非獨立推導」）：**
    - 測試檔內定義固定種子 LCG（seed 42，x=(x*1664525+1013904223)>>>0，price=100+(x/2^32)*10）產生 60 根 closes（KDJ 另以 close±1 造 highs/lows）。
    - 先用臨時腳本（node 或 vitest 單跑）以現行實作算出 calculateMACD(series) 的 macdLine[59]/signalLine[59]/histogram[59] 與 calculateKDJ 的 K[59]/D[59]/J[59]，把數值貼進 toBeCloseTo(值, 8) 斷言。臨時腳本不留檔。
  </behavior>
  <action>
    用 Write 工具建立 utils/math.test.ts 實作上述斷言（依 D-3 規格，測試檔與受測檔同層——CONVENTIONS.md 無測試慣例，採 vitest 預設 include 涵蓋的同層式）。黃金值產生流程：先寫好 LCG 與斷言骨架、跑一次取實際輸出、回填數字、再跑確認綠。不改 utils/math.ts。
  </action>
  <verify>
    <automated>npx vitest run utils/math.test.ts 全綠；npx tsc --noEmit 綠</automated>
  </verify>
  <done>六個 export 各 >=2-3 案例全綠；MACD 的 10/20/10 與 KDJ 的 period=5 由 warm-up 邊界＋預設參數等值斷言明確鎖住；(a)/(b) 兩層在檔內以 describe 與註解區分。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: utils/entryFilter.test.ts — GO/WAIT/NO_GO 決策斷言＋全套驗證＋原子 commit</name>
  <files>utils/entryFilter.test.ts</files>
  <behavior>
    顯式 import vitest API，從 `./entryFilter` import runEntryFilter（型別可另 import EntryFilterResult）。檔內寫一個 makeBar helper：給 close 與序號產生 StockDataPoint（date 依序 2026-01-01 起遞增、open=前一根 close（首根=close）、high=close+1、low=close-1、volume 預設 1000，其餘 optional 欄位不填）。

    **Case 1 — GO（解析案例，完整指定的合成多頭 fixture）：**
    16 根 closes：100, 103, 106, 109, 112, 110, 108, 106, 109, 112, 115, 118, 116, 114, 117, 121。
    此序列經 detectSwings(k=2) 產生轉折 H@idx4(112)、L@idx7(106)、H@idx11(118)、L@idx13(114) → 頭頭高＋底底高＝多頭。
    末根（idx15）覆寫欄位：open=118（紅K）、volume=1500（昨量 1000 → 量比 1.5 >=1.3 攻擊量）、ma5=116、ma10=112、ma20=110、ma60=100、ma5Dir='up'、ma10Dir='up'、ma20Dir='up'、k=70、d=60、macdHist=0.5；idx14 覆寫 k=55、d=58、macdHist=0.2（前根供黃金交叉／紅柱轉強判定）。
    呼叫 runEntryFilter('TEST', data)（weeklyData/volumeProj 不傳）。
    推導鏈（供斷言依據）：chgPct=(121-117)/117≈3.42%>2、收過前高（121>118=idx14.high）、過5均、站上月線（bias≈10%）、3線多排向上、upStreak=2（idx13→12 下跌截斷，避開戒律2）、SOP 6/6、無戒律、entryPattern='回後買上漲'。
    斷言：decision==='GO'、entryPattern==='回後買上漲'、confidence===90（80+10）、trend==='多頭'、preceptHits 空陣列、stopPrice===114.95（121*0.95）、guardMaLabel==='中長線MA20'、maGuardPrice===110。
    追加 guardLevel 案例：同 fixture 以 guardLevel='MA5' 呼叫 → maGuardPrice===116、guardMaLabel==='短線MA5'。

    **Case 2 — WAIT（解析案例）：** 同 GO fixture 但末根 volume=1000（量比 1.0、5日均量比 1.0 → 無攻擊量 → SOP④✗ 且 entryPattern 因缺攻擊量變'皆不符'）。斷言：decision==='WAIT'、entryPattern==='皆不符'、confidence < 90。

    **Case 3 — NO_GO（解析案例）：** 20 根嚴格遞減 closes（如 140 起每根 -2），不填 ma 欄位。嚴格遞減序列無 fractal 轉折 → trend==='資料不足' → 非多頭。斷言：decision==='NO_GO'、confidence <= 30。

    **(b) 黃金值鎖（可選加分，若加需註解標明）：** GO fixture 的 steps 各 status 快照（['pass','pass','pass','pass','pass','pass']）。
  </behavior>
  <action>
    用 Write 工具建立 utils/entryFilter.test.ts。若 GO fixture 實跑結果與推導鏈不符（例如某步 warn 導致 SOP<6），**不改 entryFilter.ts**：先檢查 fixture 欄位是否漏填（優先），仍不符則微調 fixture 數值使其明確通過，並在 SUMMARY 記錄推導與實作的出入。

    測試綠後執行 D-3 完整驗收（Bash 工具）：
    1. `npm run test` 全綠（math＋entryFilter 一鍵）。
    2. `npx tsc --noEmit` 綠。
    3. `npm run build` 綠。
    4. `grep -r "AIza" dist/` 無結果（金鑰紅線）。
    5. `grep -rl "vitest" dist/` 與 `grep -rl "describe(" dist/` 皆無結果（測試不進 bundle）。
    全過後單一原子 commit：`git add package.json package-lock.json utils/math.test.ts utils/entryFilter.test.ts`（如有新增 vitest.config.ts 一併加入），commit message：`test(phase-d): D-3 最小單元測試——vitest 跑道＋math/entryFilter 行為鎖（MACD 10,20,10／KD 5,3）`。SUMMARY 不 commit。確認 `git status` 無其他測試相關未追蹤檔。
  </action>
  <verify>
    <automated>npm run test 全綠 && npx tsc --noEmit && npm run build && grep -r "AIza" dist/ 回空 && git log -1 --stat 顯示恰好 4（或含 config 5）個檔案</automated>
  </verify>
  <done>GO/WAIT/NO_GO 三決策路徑各有明確合成輸入斷言、停損雙軌欄位（stopPrice/maGuardPrice/guardMaLabel）被鎖住、全套驗收綠、單一原子 commit 完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | 安裝 vitest 引入第三方程式碼（僅測試期，不進 bundle） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ob4-SC | Tampering | npm install vitest | mitigate | 安裝前 `npm view vitest name repository.url` 驗證套件身分（精確名 vitest、repo vitest-dev/vitest）；vitest 為 Vite 官方生態主流框架屬 [OK] 級，無需人工 checkpoint；鎖 minor 版避免驚喜升版 |
| T-ob4-01 | Information Disclosure | dist/ bundle | mitigate | build 後 grep AIza 無結果（既有紅線）＋grep vitest/describe( 確認測試碼不進 bundle |
</threat_model>

<verification>
1. `npm run test` 一鍵全綠（vitest run，兩個測試檔）。
2. `npx tsc --noEmit` 綠（測試檔顯式 import vitest API，tsconfig 零改動下通過）。
3. `npm run build` 綠；`grep -r "AIza" dist/` 與 `grep -rl "describe(" dist/` 皆無結果。
4. `git diff HEAD~1 --stat` 只含 package.json、package-lock.json、兩個 test 檔（±vitest.config.ts）；utils/math.ts 與 utils/entryFilter.ts 零改動。
5. MACD 測試含 warm-up 邊界（idx18/19、idx27/28）與預設參數等值斷言；KDJ 測試含 idx0..3 恆 50 與預設 period 等值斷言——非標準參數 10,20,10／5,3 被行為鎖住。
</verification>

<success_criteria>
- D-3 驗收達成：npm script 一鍵可跑、全綠；math.ts 每個關鍵函式 >=2-3 已知輸入輸出案例；entryFilter 主判斷函式（runEntryFilter）GO/WAIT/NO_GO 皆有斷言。
- 專案首條測試跑道建立（vitest 3.x 鎖 minor、相容 vite@6），tsconfig／index.html／受測本體零改動。
- SUMMARY 誠實區分 (a) 解析案例與 (b) 黃金值回歸鎖，並列出過程中發現的疑似 bug（如 RSI 常數序列 NaN）供後續處置，不在本包修。
</success_criteria>

<output>
完成後建立 `.planning/quick/260713-ob4-d-3-utils-math-ts-macd-10-20-10-kd-5-3-e/260713-ob4-SUMMARY.md`（不 commit）。
</output>
