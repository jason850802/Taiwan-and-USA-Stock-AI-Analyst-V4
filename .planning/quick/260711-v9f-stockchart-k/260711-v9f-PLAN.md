---
phase: quick-260711-v9f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/StockChart.tsx
  - App.tsx
autonomous: true
requirements:
  - ISSUE-1-limit-board-candle
  - ISSUE-2-chip-min-bar
  - ISSUE-3-interval-loading
user_setup: []

must_haves:
  truths:
    - "一字板（漲跌停鎖死，high===low）的 K 棒在圖上可見：至少一條最小可見水平線"
    - "一字板水平線顏色依『與前收比較』決定：漲停紅、跌停綠、無變化灰"
    - "外資/投信小量買賣超柱有最小可見高度，正負方向與紅買綠賣顏色正確"
    - "切換 K 棒週期時圖表區（K線＋副圖面板）出現繁中『載入中…』動畫，資料就緒後移除"
    - "既有殭屍棒過濾、拖曳平移、十字線、React.memo 效能行為不受影響"
  artifacts:
    - path: "components/StockChart.tsx"
      provides: "一字板水平線渲染 + 法人小量柱最小高度渲染"
      contains: "CandleStickShape"
    - path: "App.tsx"
      provides: "切週期時 StockChart 區域 loading 覆蓋層"
      contains: "載入中"
  key_links:
    - from: "components/StockChart.tsx CandleStickShape"
      to: "payload.priceChange"
      via: "一字板水平線顏色判斷（與前收比較）"
      pattern: "priceChange"
    - from: "components/StockChart.tsx 法人/投信 Bar"
      to: "自訂 shape（最小高度＋正負色）"
      via: "shape prop"
      pattern: "shape="
    - from: "App.tsx loading state"
      to: "StockChart 區域 overlay"
      via: "loading && <overlay>"
      pattern: "loading &&"
---

<objective>
修正使用者回報的三個 StockChart 顯示問題（dev 環境已開，可實測）：
1. 漲跌停一字板 K 棒完全不顯示。
2. 外資與投信買賣超柱狀圖在數量偏低時看不見。
3. 切換 K 棒週期時缺少讀取動畫、UI 像當機。

Purpose: 讓一字板、小量法人買賣超都能被辨識，切週期時給明確視覺回饋，提升圖表可信度與可用性。
Output: `components/StockChart.tsx`（問題 1、2）與 `App.tsx`（問題 3）修正；純渲染／UI 層變更，不新增依賴、不改資料契約（`StockDataPoint[]` 不變）。
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md

# 待修檔案
@components/StockChart.tsx
@App.tsx

# 相容性參考（勿改）：260711-unf 殭屍棒過濾器保留「漲跌停零量棒」，一字板資料已到達圖表，只差渲染
# services/yahoo.ts 4.5 步過濾器（約 698–718 行）：isZombie 僅剔除「量0 且 O=H=L=C=前一保留棒收盤」，價≠前收之一字板不受影響
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修正一字板（漲跌停鎖死）K 棒不顯示</name>
  <files>components/StockChart.tsx</files>
  <action>
    修正 `CandleStickShape`（約 47–95 行）。根因：guard `if (!open || !close || width <= 0 || height <= 0) return null;`（約 52 行）——一字板 high===low 時 recharts 傳入的 bar 像素 `height` 為 0，於是在任何繪製碼之前就 return null，K 棒完全不顯示。

    改法：把 `height <= 0` 從「直接 return null」的條件移出，改為在 `height <= 0 || high === low` 時走「一字板水平線」分支：以 `y`（此時等於該價位的像素位置）為基準，畫一條從 `x` 到 `x + width` 的水平線，`strokeWidth={2}`（最小可見高度）。保留 `!open || !close || width <= 0` 作為無效資料 guard（價格永不為 0，可續用）。

    顏色依「與前收比較」決定（不是 close vs open——一字板 close===open 會判成灰）：讀 `payload.priceChange`（displayData 已於每根附上，見 StockChart displayData 映射的 `priceChange` 欄位）。`priceChange > 0` → 漲停紅 `#f0405a`；`< 0` → 跌停綠 `#22c55e`；`=== 0` → 灰 `#94a3b8`。沿用檔內既有台股紅漲綠跌色票。

    務必維持相容：一字板是真實成交（漲跌停鎖死、量通常不為 0），與 260711-unf 修的颱風假假平盤棒（量0、價=前收）不同；殭屍過濾器已保留一字板，本 task 只補渲染，不得新增任何過濾/剔除邏輯。既有 doji（bodyHeight < 1）分支與正常 K 棒分支維持不變。
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>dev 環境開一檔近期有漲停或跌停一字板的台股（或用歷史區段拖曳到該日），確認一字板顯示為一條水平線：漲停紅、跌停綠；正常 K 棒與十字線／拖曳行為不變。</human-check>
  </verify>
  <done>tsc 無新增錯誤；一字板呈現最小可見水平線且顏色依漲跌正確；正常 K 棒渲染不變。</done>
</task>

<task type="auto">
  <name>Task 2: 修正外資/投信買賣超小量柱不顯示</name>
  <files>components/StockChart.tsx</files>
  <action>
    問題：`SubPanelChart` 的 `foreign`（約 464 行）與 `trust`（約 475 行）`<Bar>` 依 YAxis 自動縮放，比例被大值主導，小量買賣超柱高趨近 0px 看不見。

    改法：新增一個模組層級自訂 shape 元件（放在檔案 Section 1「Custom Shapes」附近，與 CandleStickShape 同區），對「非零」值強制最小像素高度並保持正負方向與顏色：
    - 讀該柱數值（透過兩個薄包裝 shape 元件分別把 payload 對應欄位餵入：外資讀 `payload.foreignBuySell`、投信讀 `payload.investmentTrustBuySell`，避免依賴 recharts 傳入的 `value` 型別歧義）。
    - 值為 `null`/`undefined`/`0` → return null（維持零買賣超不畫，與現況一致）。
    - 顏色自帶（不依賴 Cell）：值 `> 0` 紅買超 `#f0405a`、`< 0` 綠賣超 `#22c55e`（沿用檔內 foreignCells/trustCells 既有色邏輯）。
    - 最小高度 `MIN_H = 2`：`height >= MIN_H` 照原 `x/y/width/height` 畫 rect；`height < MIN_H` 時撐到 MIN_H，方向依正負錨定零線——正值 `ry = y + height - MIN_H`（自零線往上撐）、負值 `ry = y`（自零線往下撐），height 皆設 MIN_H。（recharts：正值 rect 底邊、負值 rect 頂邊皆貼零線，故此錨定正確。）

    將兩個 `<Bar>` 改為 `shape={<ForeignBar />}` / `shape={<TrustBar />}`，`isAnimationActive={false}` 保留。移除這兩個 Bar 的 `{foreignCells}` / `{trustCells}` Cell 子元素（顏色已由 shape 自帶），並一併移除已無用的 `foreignCells`、`trustCells` `useMemo`（約 750–756 行）、`SubPanelChartProps` 介面中的 `foreignCells`/`trustCells` 欄位、`SubPanelChart` 解構參數、以及父層 JSX 傳入這兩個 prop 的地方，避免留死碼。`macdHistCells`（MACD 用）與主圖 `volumeCells` 維持不動。ReferenceLine y=0 維持。
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>dev 環境選一檔有籌碼、且某些日法人買賣超量很小的台股，切到外資/投信副圖，確認小量柱可見、方向（零線上下）與紅買綠賣顏色正確；零買賣超日不畫柱。</human-check>
  </verify>
  <done>tsc 無新增錯誤；外資/投信小量買賣超柱有最小可見高度、正負方向與顏色正確；無殘留 foreignCells/trustCells 死碼。</done>
</task>

<task type="auto">
  <name>Task 3: 切換 K 棒週期時加入讀取動畫</name>
  <files>App.tsx</files>
  <action>
    問題：`fetchData`（108–124 行）在切週期時（useEffect 依賴 `interval`，126–128 行）設 `loading=true` 但不清空 `data`，舊圖凍結、無任何回饋，使用者以為當機。`loading` state 已存在（25/28 行），StockChart 未收 loading prop。

    改法（沿用既有 App.tsx 集中式 loading state，維持 StockChart 為純呈現元件）：在 `data.length > 0` 區塊（約 379–392 行）內、`<ChartToolbar>` 之後，將 `<StockChart .../>` 包一層 `className="relative"` 的 div，並在其中條件渲染覆蓋層 `{loading && (...)}`：絕對定位 `absolute inset-0 z-20`、半透明底 `bg-slate-900/60 backdrop-blur-sm`、置中 flex-col，內含 `<Loader2 className="animate-spin text-blue-400" size={32} />` 與繁中文字 `載入中…`（`text-slate-300 text-sm`）。此覆蓋層同時蓋住 K 線圖與其下的成交量/指標/法人副圖（皆在 StockChart 內），一次滿足需求；不覆蓋 ChartToolbar 讓使用者仍可再切週期。

    沿用專案既有 spinner 慣例（`Loader2` + `animate-spin`，見 StockSearch.tsx:152）。確認 `Loader2` 已在 App.tsx 的 `lucide-react` import 內，若無則加入。z-index 需高於 StockChart 內部縮放鈕（z-10）。
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>dev 環境載入一檔股票後，切換日/週/月（或分K）週期，確認圖表區立即出現「載入中…」spinner，資料就緒後消失；快速連切不殘留。</human-check>
  </verify>
  <done>tsc 無新增錯誤；切週期期間 StockChart 區域顯示載入動畫、就緒後移除；ChartToolbar 仍可操作。</done>
</task>

</tasks>

<threat_model>
純前端渲染／UI 變更，無新增依賴、無新增外部輸入或信任邊界；不觸及金鑰、資料抓取、prompt。無新 STRIDE 威脅。既有殭屍棒過濾（資料完整性防線）不得被弱化——本計畫僅補 UI 呈現，未改任何過濾/資料層邏輯。
</threat_model>

<verification>
- `npx tsc --noEmit` 全綠（無新增型別錯誤；專案 tsconfig 非 strict）。
- dev 實測三項：一字板可見且色正確、法人小量柱可見且方向色正確、切週期有載入動畫。
- 回歸檢查：拖曳平移、十字線、收盤價游標線、縮放、正常 K 棒、MACD/KDJ/RSI 副圖行為不變。
</verification>

<success_criteria>
- 漲跌停一字板 K 棒以最小可見水平線呈現，顏色依與前收比較（漲紅/跌綠/平灰）。
- 外資/投信小量買賣超柱有最小可見高度，正負方向與紅買綠賣顏色正確；零值不畫。
- 切換 K 棒週期時圖表區顯示繁中「載入中…」動畫，資料就緒後移除。
- 不新增依賴（package.json / index.html importmap 皆不動）；`StockDataPoint[]` 契約不變；殭屍棒過濾不受影響。
</success_criteria>

<output>
完成後建立 `.planning/quick/260711-v9f-stockchart-k/260711-v9f-SUMMARY.md`
</output>
