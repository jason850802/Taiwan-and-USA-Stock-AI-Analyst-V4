---
phase: 260712-v6l-b-2-search-ux-three-fixes-local-first-re
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/stockDirectory.ts, components/StockSearch.tsx]
autonomous: true
requirements: [QUICK-260712-v6l]

must_haves:
  truths:
    - "本地結果不等 Yahoo：searchStocks 兩段式，本地命中在 searchYahoo await 之前發射（程式層保證 <300ms 上屏：debounce 180ms＋本地搜尋同步，local 相位無任何遠端 await）"
    - "名錄載入完成前輸入也能得到本地結果：searchStocks 內部自行 await ensureTaiwanDirectory()，不再吃外部傳入的 dir snapshot（競態根除）"
    - "「找不到符合」只在終態出現：名錄未就緒顯示「載入名錄中…」、查詢進行中（searching）不顯示找不到、僅 dirReady＋!searching＋空結果的終態才顯示"
    - "純中文搜尋仍 0 網路請求：hasCJK 短路保留，CJK 查詢單發 final、不呼叫 searchYahoo"
    - "無過期結果錯置：local 與 final 兩個相位的發射在消費端皆過 reqId 檢查，過期一律丟棄"
    - "A1 不退化：mapYahooQuote 白名單、isSearchableTaiwanEntry 過濾、合併 15 筆上限、Market 'TW'|'US'、bare-suffix 去重全數原樣"
    - "npx tsc --noEmit 通過"
  artifacts:
    - path: "services/stockDirectory.ts"
      provides: "兩段式 searchStocks(query, onResults) — 內部 ensureTaiwanDirectory、local/final 相位發射契約"
      contains: "onResults"
    - path: "components/StockSearch.tsx"
      provides: "dirReady state＋三態下拉面板（結果清單／載入名錄中／找不到符合）＋callback 消費端 reqId 防過期"
      contains: "載入名錄中"
  key_links:
    - from: "services/stockDirectory.ts searchStocks"
      to: "ensureTaiwanDirectory"
      via: "函式內部 await（memCache 第二次起零成本），取代外部 dir 參數"
      pattern: "await ensureTaiwanDirectory"
    - from: "components/StockSearch.tsx runSearch"
      to: "searchStocks onResults callback"
      via: "callback 內先檢查 myId !== reqIdRef.current 再 setResults；phase==='final' 才 setSearching(false)"
      pattern: "reqIdRef.current"
    - from: "components/StockSearch.tsx 下拉面板"
      to: "dirReady / searching 狀態"
      via: "三態條件：results>0 出清單；空結果時 !dirReady→載入名錄中、searching→不顯示、終態→找不到"
      pattern: "dirReady"
---

<objective>
B-2 搜尋 UX 三修。實作規格已由 `.planning/optimization/PLAN.md` §Phase B / B-2 鎖定（根因已定位，照做、不重新設計）：
1. 本地結果被 Yahoo 扣住（B-2 修法1）→ searchStocks 改兩段式：本地命中立即上屏，Yahoo 到貨再併入。
2. 「找不到符合」誤閃（B-2 修法2）→ 三態面板：載入名錄中／查詢中不判／終態才顯示找不到。
3. 名錄就緒競態（B-2 修法3）→ searchStocks 內部自行 await ensureTaiwanDirectory()。

**API 形狀（planner 裁決）**：callback 式兩相位發射
`searchStocks(query: string, onResults: (results: StockDirEntry[], phase: 'local' | 'final') => void): Promise<void>`。
理由：最小侵入——消費端既有 debounce＋reqId 模式 1:1 映射到 callback；merge/去重/15 筆上限仍由單一函式持有；
不引入 AsyncGenerator（消費端要改寫 for await）也不用雙 promise 物件（local 相位因內部 ensureTaiwanDirectory 本身是 async，形狀彆扭）。

Purpose: 影片實測搜「6449」輸入後 ~6 秒才出現下拉，而結果最終來自本地名錄——0ms 的本地命中被 Yahoo 冷握手（3-8 秒）扣住；
中間態誤現「找不到符合」誤導使用者；名錄載入前輸入吃到空 dir 且到貨不重跑。
Output: `services/stockDirectory.ts` 兩段式搜尋 ＋ `components/StockSearch.tsx` 消費端接線與三態面板 ＋
一次性 Node 斷言驗證（用完即刪，不 commit）。

**Git 紀律（紅線）**：工作樹有本任務之外的既有未提交變更（.planning/ 下多個 SUMMARY.md）。
commit 一律逐檔 `git add <path>` 只 stage 本任務實際修改的檔案，**絕不可 `git add -A` 或 `git add .`**。
</objective>

<execution_context>
@E:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
@E:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/optimization/PLAN.md
@services/stockDirectory.ts
@components/StockSearch.tsx

# 規劃時已實讀確認的事實（勿重新偵查）：
#   - searchStocks 現形（stockDirectory.ts:168-184）：吃外部 dir 參數；hasCJK 短路回 tw；
#     非 CJK 時 await searchYahoo 後 merge（bare-suffix 去重 `.replace(/\.TWO?$/i,'')`）→ slice(0,15) 一次回傳
#   - searchYahoo（:150-165）內部已 try/catch、失敗回 []——永不 throw，final 相位可據此保證必發
#   - ensureTaiwanDirectory（:27-81）：memCache → localStorage(7天TTL) → FinMind；loadingPromise 併發去重；
#     失敗 resolve 為 memCache||[]（不 throw）——內部 await 它安全且第二次起零成本
#   - StockSearch.tsx 唯一消費端（全 repo grep 確認）：dir state(:33) 由 mount effect(:43) 填入；
#     runSearch(:54-67) debounce 180ms＋reqIdRef 防過期；useCallback dep [dir]（stale closure 即競態根因）
#   - 「找不到符合」條件在 :138：`open && !searching && results.length === 0 && value.trim()`
#   - setSearching(true) 在 runSearch 同步呼叫（:57）、setOpen(true) 目前在結果到貨後（:64）
#   - A1 產物（260712-qfi，不得退化）：mapYahooQuote(:133-147) quoteType 嚴格 EQUITY/ETF＋美 7 所白名單、
#     isSearchableTaiwanEntry(:96-105)、searchTaiwan limit 15、merged.slice(0,15)、Market='TW'|'US'
#   - services/_shared/apiClient.ts node 下安全（optional-chained import.meta.env）——
#     但本次 searchStocks 內部會碰 localStorage/fetch，一次性斷言腳本須先 stub globalThis 再動態 import
#   - tsconfig 非 strict、無 test runner——驗證靠 tsc＋一次性 npx tsx 斷言腳本
</context>

<tasks>

<task type="auto">
  <name>Task 1: searchStocks 兩段式＋內部名錄載入＋消費端接線（B-2 修法1、3）</name>
  <files>services/stockDirectory.ts, components/StockSearch.tsx</files>
  <action>
    在 services/stockDirectory.ts 改寫 `searchStocks`：

    1. 新簽名：`export async function searchStocks(query: string, onResults: (results: StockDirEntry[], phase: 'local' | 'final') => void): Promise<void>`
       ——移除 dir 參數（B-2 修法3：外部 snapshot 是競態根因）。
    2. 函式開頭 `const dir = await ensureTaiwanDirectory();`（有 memCache＋loadingPromise 併發去重，
       第二次起零成本；失敗 resolve []，不需額外 try/catch）。
    3. 發射契約（明確且必守）：
       - query trim 後為空 → 不發射，直接 return（消費端已在 runSearch 前置擋掉空 query，此為防禦）。
       - `hasCJK(q)` → `onResults(searchTaiwan(dir, q, 15), 'final')` 單發後 return
         ——**保證 0 網路請求（不碰 searchYahoo），維持 A1 驗收**。
       - 非 CJK：先算 `tw = searchTaiwan(dir, q, 15)`；**僅當 tw.length > 0 才發 `onResults(tw, 'local')`**
         （空 local 發射無資訊價值，且會把前一查詢的清單瞬間塌掉再展開——不發，讓 final 一次定案）；
         接著 `const yahoo = await searchYahoo(q, 8)`（永不 throw），沿用現行 bare-suffix 去重
         （`y.id.replace(/\.TWO?$/i, '')` 對 seen 檢查）merge 後 `onResults(merged.slice(0, 15), 'final')`。
       - **final 相位無條件恰發一次**（Yahoo 空/失敗時 merged 即 tw 原樣）——消費端靠它結束 searching 態。
    4. searchTaiwan / searchYahoo / mapYahooQuote / isSearchableTaiwanEntry 一律不動
       （searchTaiwan 保留 dir 參數維持純函式可測性）。

    在 components/StockSearch.tsx 接線：

    5. 刪除 `dir` state（:33）；mount effect（:43）改為
       `ensureTaiwanDirectory().then(() => setDirReady(true)).catch(() => {});`，
       新增 `const [dirReady, setDirReady] = useState(false);`（本 task 先宣告與填值，
       render 消費在 Task 2；tsconfig 非 strict，中間態不會因 unused 報錯）。
       ——mount effect 保留是刻意的：它是名錄的預熱（prefetch），讓多數搜尋到達時 memCache 已就緒。
    6. runSearch 改寫：
       - 同步段：`setSearching(true); setOpen(true);`（open 提前到查詢開始——Task 2 的
         「載入名錄中…」等中間態面板需要掛載點；副作用是搜尋進行中點擊外部關閉後、結果到貨不再自動重開，
         此為刻意的行為改善，記入 SUMMARY）。
       - debounce 180ms 後呼叫 `searchStocks(q, (r, phase) => { ... })`，callback 內：
         `if (myId !== reqIdRef.current) return;`（**local 與 final 兩相位皆受 reqId 防過期**）；
         `setResults(r); setActive(-1);`；`if (phase === 'final') setSearching(false);`
         ——local 到貨清單即上屏而 spinner 續轉，final 到貨才收斂 searching。
       - useCallback deps 由 `[dir]` 清為 `[]`（stale closure 隨 dir state 一併根除）。
    7. import 行維持 `ensureTaiwanDirectory, searchStocks, StockDirEntry, Market`（皆仍被使用）；
       其餘（pick/submitCurrent/onKeyDown/highlight/marketBadge）一律不動。
  </action>
  <verify>
    <automated>用 Bash 工具跑：npx tsc --noEmit 通過（注意 Task 1 完成時 :138 面板條件仍是舊式——舊條件與新狀態並存仍可編譯，終態語意由 Task 2 完成）；再以一次性斷言腳本（放系統暫存區 scratchpad 或本任務目錄下、驗完即刪不 commit）用 `npx tsx` 驗證：腳本先 stub `globalThis.localStorage`（getItem→null/setItem noop）與 `globalThis.fetch`（finmind URL→延遲 50ms 回 {msg:'success',data:[2330 台積電 twse/0050 ETF twse/6488 tpex 三筆]}；yahoo/search URL→延遲 100ms 回 {quotes:[{symbol:'AAPL',quoteType:'EQUITY',exchange:'NMS',shortname:'Apple Inc.'}]}，並計數呼叫）再動態 import stockDirectory.ts，斷言：(1) 冷啟動立即 searchStocks('2330',cb)——不先呼叫 ensureTaiwanDirectory——local 相位仍含 2330（名錄競態已根除）且 local 發射時 yahoo stub 尚未 resolve（記錄時序旗標：local 必早於 yahoo 回應）；(2) final 相位恰一次且含 AAPL 併入、總數 ≤15；(3) searchStocks('台積',cb) 恰一次 final 發射、yahoo fetch 呼叫計數為 0；(4) yahoo stub 改為 reject 時 final 仍發射且結果=本地命中原樣；(5) yahoo 回 {symbol:'2330.TW',quoteType:'EQUITY'} 時 final 無重複（bare-suffix 去重原樣）。（若 npx tsx 不可用，退回 Node ≥22.6 --experimental-strip-types）</automated>
  </verify>
  <done>searchStocks 為兩段式 callback API 且內部自行 ensureTaiwanDirectory；local 相位不等 Yahoo、final 必發恰一次；CJK 0 網路請求；去重與 15 筆上限原樣；消費端兩相位皆過 reqId；dir state 已移除、useCallback deps 為 []；tsc 與 5 組斷言全過</done>
</task>

<task type="auto">
  <name>Task 2: 下拉面板三態化——終結「找不到符合」誤閃（B-2 修法2）</name>
  <files>components/StockSearch.tsx</files>
  <action>
    改寫 StockSearch.tsx 下拉面板 render 條件（現 :118-142 兩個分支），成為三態單一決策鏈：

    1. `open && results.length > 0` → 結果清單（現有 ul 分支，原樣不動）。
    2. `open && value.trim() && results.length === 0` 時依序判定（用巢狀三元或早退 helper，擇可讀者）：
       - `!dirReady` → 面板顯示「載入名錄中…」（沿用現有找不到面板的容器樣式
         `absolute z-30 ... bg-slate-800 border border-slate-600 rounded-xl ... text-sm text-slate-400`，
         僅換文案；可另加一顆 `Loader2 animate-spin` 小圖示增加載入感，非必要）。
       - `dirReady && searching` → **不渲染任何面板**（查詢進行中不判找不到；input 右側既有 spinner 已足）。
       - `dirReady && !searching` → 顯示「找不到符合「{value}」的股票」（唯一終態）。
    3. 誤閃三情境自查（寫碼後對照）：
       (a) 名錄未就緒時輸入 → searching=true＋dirReady=false → 顯示「載入名錄中…」而非找不到；
       (b) 前一查詢空結果、使用者續打字 → runSearch 同步 setSearching(true) → 查詢中分支，不顯示找不到；
       (c) 非 CJK 查詢本地 0 命中、Yahoo 未回 → 無 local 發射、searching 續為 true → 不顯示找不到，
           final 到貨才定案（有結果出清單／真空才見找不到）。
    4. 不動：results>0 清單分支、鍵盤導航（onKeyDown 對 results.length===0 的 guard 已相容）、
       onFocus 重開邏輯、點擊外部關閉、marketBadge、highlight。
  </action>
  <verify>
    <automated>用 Bash 工具跑：npx tsc --noEmit 通過；grep -n "載入名錄中" components/StockSearch.tsx 恰 1 處命中；grep -n "找不到符合" components/StockSearch.tsx 恰 1 處且該分支條件同時含 dirReady 與 !searching（讀該行前後 10 行人工比對三態決策鏈與 action 第 2 點一致）；對照 action 第 3 點三情境逐一走讀狀態流轉確認無誤閃路徑</automated>
  </verify>
  <done>三態面板成立：載入名錄中（!dirReady）／查詢中無面板（searching）／終態才見找不到；三個誤閃情境走讀全數封死；結果清單與鍵盤導航行為零變化；tsc 通過</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 瀏覽器 → Yahoo search 回應（經同源代理） | 外部不受信資料經 callback 直接餵進搜尋建議清單 |
| 瀏覽器 → FinMind 名錄（經同源代理＋localStorage 快取） | 外部名錄資料本地快取 7 天 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v6l-01 | Tampering | searchYahoo → final 相位發射 | mitigate | A1 白名單原樣沿用：mapYahooQuote quoteType＋交易所雙白名單，畸形 quote 回 null 丟棄；本任務不放寬任何過濾 |
| T-v6l-02 | DoS | 兩段式後的重複請求 | mitigate | debounce 180ms＋reqId 丟棄過期原樣保留；hasCJK 短路維持 CJK 查詢 0 網路請求；searchYahoo 每查詢仍恰 1 次 |
| T-v6l-SC | Tampering | 依賴安裝 | accept | 本任務零新依賴（package.json/importmap 皆不動）；npx tsx 僅一次性驗證工具，不進 repo，可用 Node 原生 --experimental-strip-types 替代 |
</threat_model>

<verification>
1. `npx tsc --noEmit` 通過（兩個 task 各自驗、收尾再驗一次）。
2. Task 1 一次性 Node 斷言 5 組全過（兩段式時序、名錄競態根除、CJK 0 請求、Yahoo 失敗仍發 final、去重）。
3. 程式碼審視對照 §B-2 驗收清單：本地上屏 <300ms 的程式層保證（local 相位無遠端 await、debounce 180ms）；
   「找不到符合」僅終態；名錄載入前輸入可得本地結果；CJK 0 網路請求；美股/Yahoo 併入且 reqId 防錯置；
   A1 迴歸守則（mapYahooQuote 白名單/15 筆/Market TW|US）以 git diff 確認未觸碰。
   不強制啟動 dev server——e2e 體感驗收（連點 5 檔無錯置、實測 <300ms）屬 Phase B 收尾的
   Sonnet 覆核＋preview 實跑範圍（optimization/PLAN.md 全案共同驗收），非本包阻斷項。
4. `git status` 確認只 stage services/stockDirectory.ts、components/StockSearch.tsx 與本任務
   .planning/quick/260712-v6l-b-2-search-ux-three-fixes-local-first-re/ 文件；一次性斷言腳本已刪除。
</verification>

<success_criteria>
- §B-2 三修全數落地，無任何一項被簡化或延後
- must_haves.truths 全數成立
- A1（260712-qfi）搜尋限縮零退化（diff 層面可證：過濾函式與上限未觸碰）
- 原子 commit 只含本任務檔案（絕無 git add -A）
</success_criteria>

<output>
完成後建立 `.planning/quick/260712-v6l-b-2-search-ux-three-fixes-local-first-re/260712-v6l-SUMMARY.md`
（含：API 形狀裁決與理由、發射契約、斷言結果、setOpen 提前的行為改善註記、commit hash）。
</output>
