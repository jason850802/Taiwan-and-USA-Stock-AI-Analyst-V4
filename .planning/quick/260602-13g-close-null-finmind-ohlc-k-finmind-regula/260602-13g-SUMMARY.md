---
phase: quick-260602-13g
plan: 01
subsystem: data
tags: [yahoo-finance, finmind, stock-data, typescript, data-layer]

# Dependency graph
requires:
  - phase: quick-260602-0u1
    provides: "processYahooResult 1d 最新一根 null-close 以 regularMarketPrice 平盤合成補值"
provides:
  - "台股最新一根 null-close 合成棒，若 FinMind 有當日真實 OHLC 則以真實 K 棒取代（含 Adj）"
  - "processYahooResult 合成棒內部 _synthetic 標記（步驟4 收尾即移除，不外洩）"
  - "getStockData 步驟3 ohlcMap（FinMind open/max/min/close）"
affects: [yahoo-data-layer, dashboard, entry-filter, kline-signal, volume-analysis]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "平盤合成棒以內部 _synthetic 標記，步驟4 用 FinMind 真實 OHLC 取代，return 前 delete 標記避免外洩"
    - "FinMind OHLC 欄位對映 high=item.max / low=item.min（比照 fetchFinMindDailyData）"

key-files:
  created: []
  modified: [services/yahoo.ts]

key-decisions:
  - "_synthetic 為內部欄位（push 在 any-typed cleanData），不進 types.ts，return 前以 delete 清除"
  - "取代條件 d._synthetic === true && ohlcMap.has(d.rawDateStr)：精準鎖定平盤合成棒，且僅 FinMind 有當日資料時觸發"
  - "volume 不在取代分支重複處理——既有 volumeMap 機制已覆寫合成棒原 volume=0"

patterns-established:
  - "分層 fallback：TW + FinMind OHLC → 真實棒；US / 盤中 / fallback / 非1d → 維持前一任務平盤合成補值不退化"

requirements-completed: [BUGFIX-yahoo-latest-null-close-finmind-ohlc]

# Metrics
duration: 4min
completed: 2026-06-01
---

# Phase quick-260602-13g Plan 01: FinMind 真實 OHLC 取代平盤合成棒 Summary

**延伸 260602-0u1：台股最新一根日線 close=null 的 regularMarketPrice 平盤合成棒，若 FinMind 該日已有真實開高低收，改以 FinMind 真實 OHLC 取代，使最新一根為真實 K 棒；FinMind 無當日資料才維持平盤補值。**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-01T16:51:25Z
- **Completed:** 2026-06-01T16:55:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- `processYahooResult` 的 1d 平盤合成棒物件新增內部欄位 `_synthetic: true`（僅平盤分支；forEach 真實棒不加任何標記）。
- `getStockData` 步驟3 新增 `ohlcMap`（與 `volumeMap` 同層級宣告），於 `finMindPriceData.forEach` 內以 `{ open: item.open, high: item.max, low: item.min, close: item.close }` 建表（high/low 取 max/min，比照 `fetchFinMindDailyData`）。
- `getStockData` 步驟4「Final Enriching」map 內，在既有 volume 覆寫之後加入真實 OHLC 取代分支：`if (d._synthetic === true && ohlcMap.has(d.rawDateStr))` 覆寫 `open/high/low/close` 與對應 `openAdj/highAdj/lowAdj/closeAdj`（ratio=1，Adj 等於原值）。
- 收尾 `delete d._synthetic`（在 `...d` 展開前執行），確保內部標記絕不洩漏進 `fullProcessedData` / 對外 `StockDataPoint`。
- `types.ts` 契約零變動（`_synthetic` 存在於 any-typed 中間資料，不進入回傳型別）。

## Task Commits

Each task was committed atomically:

1. **Task 1: 合成棒加 _synthetic 標記 + 步驟3 建 ohlcMap** - `dc67507` (feat)
2. **Task 2: 步驟4 用 FinMind 真實 OHLC 取代合成棒 + 收尾移除 _synthetic** - `73467de` (feat)

## Files Created/Modified

- `services/yahoo.ts` - 合成棒加 `_synthetic` 標記；步驟3 新增 `ohlcMap` 並於 FinMind forEach 寫入；步驟4 新增真實 OHLC（含 Adj）取代分支 + `delete d._synthetic` 收尾。

## Decisions Made

- **`_synthetic` 為純內部欄位**：push 在 any-typed `cleanData`，不加入 `types.ts`；return 前以 `delete` 清除，避免下游 `...d` 展開洩漏。
- **取代條件精準**：`d._synthetic === true && ohlcMap.has(d.rawDateStr)` 同時鎖定「平盤合成棒」與「FinMind 有當日資料」兩條件，缺一不取代。
- **volume 不重複處理**：合成棒原 `volume=0`，既有 `volumeMap` 覆寫機制已在同一 map 內先行處理；取代分支只動 OHLC/Adj。

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. `npx tsc --noEmit` 兩次（Task 1 後、Task 2 後）皆以 exit 0 通過，全專案零型別錯誤。

## Verification Results

- **tsc --noEmit:** PASSED（exit 0，兩次）。
- **`_synthetic` grep（services/yahoo.ts）:** 僅出現於：標記 push（行 467）、步驟4 取代條件（行 697）、`delete`（行 712），其餘為註解。對外回傳物件無殘留 → types.ts 零變動。
- **分層不變式（程式邏輯複查）:**
  1. 台股最新棒 close=null + FinMind 有當日 OHLC → `_synthetic && ohlcMap.has` 為真 → open/high/low/close 為 FinMind 真實值（含 Adj），volume 由 volumeMap 覆寫為真實量。
  2. 台股最新棒 close=null + FinMind 當日無資料 → `ohlcMap.has` 為 false → 不取代 → 維持 regularMarketPrice 平盤棒（行為不退化）。
  3. 美股最新棒 close=null → `shouldFetchFinMindChips=false`（非台股）→ ohlcMap 空 → 平盤棒維持，等同前一任務。
  4. FinMind fallback 路徑（usedFallback）→ processedData 無 `_synthetic` 標記 → `delete` 為 no-op、取代分支不觸發 → 真實 OHLC 串不被污染。
  5. 中間零星 null / 週月分鐘線 → 不產生 `_synthetic`（標記只在 1d 平盤分支產生）→ 不受影響。
- **後置刪除檢查:** `git diff --diff-filter=D HEAD~2 HEAD` 無任何檔案刪除。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 資料層改動完成；`StockDataPoint` 契約零變動，下游圖表 / 進場過濾器 / 提示詞無需調整。
- 待 Vercel 後端代理層上線後，此 FinMind OHLC 取代邏輯隨資料服務層遷移時須保持行為一致（後端仍回原始 JSON，補值與取代皆在前端 `getStockData` 內）。

## Self-Check: PASSED

- FOUND: services/yahoo.ts (modified — `_synthetic` 標記、`ohlcMap`、步驟4 取代分支、`delete d._synthetic` 皆在位)
- FOUND: commit dc67507
- FOUND: commit 73467de

---
*Phase: quick-260602-13g*
*Completed: 2026-06-01*
