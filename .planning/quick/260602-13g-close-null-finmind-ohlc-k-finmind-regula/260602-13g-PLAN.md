---
phase: quick-260602-13g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/yahoo.ts]
autonomous: true
requirements: [BUGFIX-yahoo-latest-null-close-finmind-ohlc]
tags: [yahoo-finance, finmind, stock-data, typescript, data-layer]

must_haves:
  truths:
    - "台股最新一根日線 close 為 null 時，若 FinMind 有當日真實 OHLC，dashboard 最新一根顯示 FinMind 真實 K 棒（open/high/low/close 各不相同的真實值），而非平盤合成棒"
    - "FinMind 當日無資料時（美股、或台股盤中尚無當日 FinMind 資料），維持前一任務的 regularMarketPrice 平盤合成補值，行為不退化"
    - "對外回傳的 StockDataPoint[] 不含 _synthetic 內部標記，types.ts 契約零變動"
    - "只影響最新一根、原 close 為 null、interval==='1d' 的情況；中間零星 null、週/月/分鐘線、FinMind fallback 整串路徑皆不受影響"
  artifacts:
    - path: "services/yahoo.ts"
      provides: "processYahooResult 合成棒標記 _synthetic + getStockData 步驟3 ohlcMap + 步驟4 真實 OHLC 取代 + 收尾移除 _synthetic"
      contains: "_synthetic"
  key_links:
    - from: "processYahooResult 合成 K 棒"
      to: "getStockData 步驟4 enriching"
      via: "_synthetic 標記 + rawDateStr 對齊 ohlcMap"
      pattern: "_synthetic"
    - from: "getStockData 步驟3 finMindPriceData.forEach"
      to: "ohlcMap"
      via: "item.date → {open, max, min, close}"
      pattern: "ohlcMap"
---

<objective>
延伸前一個 quick task（260602-0u1）：台股最新一根日線 close 為 null 時，目前以 regularMarketPrice 合成「平盤 K 棒」(open=high=low=close)。本任務在其上加一層「FinMind 真實 OHLC 優先」——若 FinMind 該日已有真實開高低收，用真實 OHLC 取代平盤合成棒，使最新一根為真實 K 棒；FinMind 無資料才維持平盤補值。

Purpose: 平盤合成棒會讓最新一根的 K 線型態、振幅、K 棒轉折判讀失真（六六大順第 3 步 K 線轉折、第 5 步量價）。台股盤後 FinMind 通常已有當日真實 OHLC，優先採用可讓最新一根的進場判斷更貼近真實行情。
Output: services/yahoo.ts 三處改動 + 收尾移除內部標記，StockDataPoint 對外契約零變動。
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@types.ts
@services/yahoo.ts
@.planning/quick/260602-0u1-services-yahoo-ts-k-close-null-regularma/260602-0u1-SUMMARY.md

# 關鍵既有實作位置（執行時務必先讀對齊，不要重寫整個檔）
# - processYahooResult 合成補值區塊：services/yahoo.ts 約 421-467 行
#   （push 一根 open=high=low=close=rmp、volume=0 的合成棒；rawDateStr 已由 getExchangeTime 正確帶入）
# - getStockData 步驟 3 FinMind 抓取：約 648-675 行
#   （finMindPriceData.forEach 目前只 volumeMap.set(item.date, item.Trading_Volume)，第 672-674 行）
# - getStockData 步驟 4 enriching：約 677-691 行
#   （processedData.map；第 681-683 行已用 volumeMap 覆寫 d.volume）
# - FinMind fallback 路徑（usedFallback）：約 526-551 行（整串來自 FinMind，本就有真實 OHLC，不可套合成補值）
# - fetchFinMindDailyData 既有欄位對映參考：約 253-268 行（open=d.open / high=d.max / low=d.min / close=d.close / volume=d.Trading_Volume）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 合成棒加 _synthetic 標記 + 步驟3 建 ohlcMap</name>
  <files>services/yahoo.ts</files>
  <action>
  兩處小改動，維持既有分層語意，不重寫整個函式：

  (A) processYahooResult 合成補值區塊（約 445-465 行那段 cleanData.push({...}) 的物件）：
  在 push 的合成 K 棒物件內，新增一個內部欄位 `_synthetic: true`，標示「這根是用 regularMarketPrice 平盤補的、待真實 OHLC 取代」。
  不要動該物件其他欄位（open/high/low/close=rmp、volume=0、Adj 全等 rmp、rawDateStr 維持由 getExchangeTime 帶入的 YYYY-MM-DD）。
  rawDateStr 是後面對齊 FinMind 的 key，務必確認它仍是該合成棒當日的 YYYY-MM-DD（既有程式已正確帶入，勿改動其來源）。
  只在這個「平盤合成」分支加標記；序列其他真實棒（forEach 那段，約 401-417 行）不得加任何標記。

  (B) getStockData 步驟 3 FinMind 區塊（約 658-675 行）：
  在 volumeMap 旁新增一個 `const ohlcMap = new Map<string, { open: number; high: number; low: number; close: number }>();`，
  宣告位置與 volumeMap 同層級（約 640 行 volumeMap 宣告處旁，或在 shouldFetchFinMindChips 區塊內就近宣告皆可，但須讓步驟 4 可存取——比照 volumeMap 的作用域，宣告在 chipMap/volumeMap 同一處最穩）。
  擴充 finMindPriceData.forEach（約 672-674 行）：除了既有 `volumeMap.set(item.date, item.Trading_Volume)`，再加
  `ohlcMap.set(item.date, { open: item.open, high: item.max, low: item.min, close: item.close });`。
  注意 FinMind 欄位：high 取 item.max、low 取 item.min（比照既有 fetchFinMindDailyData 第 257-258 行），不可寫成 item.high/item.low。
  只新增，不刪改 volumeMap 既有那行。
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>processYahooResult 合成棒物件含 `_synthetic: true`；getStockData 內有 `ohlcMap` 宣告且 forEach 內以 item.open/max/min/close 寫入；tsc --noEmit exit 0。</done>
</task>

<task type="auto">
  <name>Task 2: 步驟4 用 FinMind 真實 OHLC 取代合成棒 + 收尾移除 _synthetic</name>
  <files>services/yahoo.ts</files>
  <action>
  在 getStockData 步驟 4「Final Enriching」的 processedData.map 回呼內（約 678-691 行），實作真實 OHLC 取代與標記清除，維持「有 FinMind OHLC → 真實棒；否則 → 平盤補值不退化」分層：

  (1) 在既有 volume 覆寫（約 681-683 行 `if (shouldFetchFinMindChips && volumeMap.has(...)) d.volume = ...`）之後、return 物件之前，加入真實 OHLC 取代：
  條件：`if (d._synthetic === true && ohlcMap.has(d.rawDateStr))`。
  取出 `const o = ohlcMap.get(d.rawDateStr)!;`，覆寫 d.open=o.open、d.high=o.high、d.low=o.low、d.close=o.close。
  同步更新 Adj 欄位：FinMind 無還原值、ratio=1，故 d.openAdj=o.open、d.highAdj=o.high、d.lowAdj=o.low、d.closeAdj=o.close（與 fetchFinMindDailyData 第 262-265 行一致）。
  取代後該根即為真實 K 棒；其 volume 已由上面既有 volumeMap 機制處理（合成棒原 volume=0，若 FinMind 有量會被覆寫成真實量），勿在此重複處理 volume。

  (2) 收尾移除內部標記：確保 `_synthetic` 絕不進入對外回傳。
  做法：在 return 的展開物件後，不要把 `_synthetic` 帶出去。最穩的做法是在 map 回呼一開始（或取得 chips 後、return 前）執行 `delete d._synthetic;`，因為下游 fullProcessedData（約 718 行起）以 `...d` 展開，若不刪會洩漏進 StockDataPoint。
  驗證：對外 fullProcessedData / 回傳的 data 任一元素都不得含 `_synthetic`（grep 確認 return 路徑無殘留標記進入對外物件）。

  (3) 不變式守衛（程式自然滿足，無需額外 if，但執行時複查）：
  - 美股或台股盤中 FinMind 無當日資料：`ohlcMap.has(d.rawDateStr)` 為 false → 不取代 → 合成棒維持平盤值（前一任務行為不退化）。
  - FinMind fallback 路徑（usedFallback=true）：該路徑 processedData 來自 fetchFinMindDailyData、無任何 `_synthetic` 標記，且 shouldFetchFinMindChips 仍會跑但 `d._synthetic` 皆 undefined → 取代分支不觸發 → 不衝突。
  - 非 1d / 週月分鐘線：根本不會產生 `_synthetic`（標記只在 processYahooResult 的 1d 分支產生），亦不受影響。
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>步驟4 map 內有 `d._synthetic === true && ohlcMap.has(d.rawDateStr)` 的真實 OHLC（含 Adj）取代分支；map 內含 `delete d._synthetic`；對外回傳物件無 `_synthetic`；tsc --noEmit exit 0。</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` → exit 0，全專案零型別錯誤（_synthetic 為內部欄位、用 any-typed processedData，不影響 StockDataPoint 契約）。
2. 邏輯複查（不變式）：
   - 台股最新棒 close=null + FinMind 有當日 OHLC → 最新一根 open/high/low/close 為 FinMind 真實值（四值通常不相等）、Adj 同步、volume 為真實量。
   - 台股最新棒 close=null + FinMind 當日無資料 → 維持 regularMarketPrice 平盤棒（open=high=low=close=rmp）。
   - 美股最新棒 close=null → shouldFetchFinMindChips=false（非台股），ohlcMap 為空 → 平盤棒維持，行為等同前一任務。
   - FinMind fallback 路徑 → 無 _synthetic、不觸發取代、整串真實 OHLC 不被污染。
   - 中間零星 null / 週月分鐘線 → 不產生 _synthetic、不受影響。
3. 對外契約：grep `_synthetic` 僅出現在 processYahooResult 標記、步驟4 取代條件與 delete；fullProcessedData 回傳物件不含該欄位 → types.ts 零變動。
</verification>

<success_criteria>
- 台股最新一根日線 close=null 且 FinMind 有當日 OHLC 時，dashboard 最新一根為 FinMind 真實 K 棒（非平盤）。
- FinMind 無當日資料（美股 / 台股盤中）時，平盤 regularMarketPrice 補值行為與前一任務一致、無退化。
- 只影響「最新一根 + 原 close=null + interval==='1d'」；其餘 K 棒、週月分鐘線、FinMind fallback 路徑零影響。
- `_synthetic` 內部標記不外洩，StockDataPoint / types.ts 契約零變動。
- `npx tsc --noEmit` 通過（exit 0）。
- 無新依賴；僅改 services/yahoo.ts；不碰 vite.config.ts / 金鑰 / 其他 service。
</success_criteria>

<output>
Create `.planning/quick/260602-13g-close-null-finmind-ohlc-k-finmind-regula/260602-13g-SUMMARY.md` when done
</output>
