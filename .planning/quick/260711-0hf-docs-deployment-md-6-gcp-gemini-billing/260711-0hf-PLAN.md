---
phase: quick-260711-0hf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/DEPLOYMENT.md]
autonomous: true
requirements: [QUICK-260711-0hf]
must_haves:
  truths:
    - "第 6 節標題改為描述 Billing 預算與快訊（非每日配額）"
    - "文件說明為何付費層帳戶下舊的每日配額（RPD）操作找不到對應項目"
    - "文件提供可重做的 Billing → Budgets & alerts 操作路徑（範圍鎖定 project、每月固定金額、50/90/100% 門檻、email 通知、無自動化斷線動作）"
    - "已移除只適用於舊配額做法的 RPD 太平洋午夜重置說明"
    - "docs/DEPLOYMENT.md 其他章節與新內容一致（無殘留每日配額交叉引用）"
  artifacts:
    - path: "docs/DEPLOYMENT.md"
      provides: "第 6 節：GCP Gemini 財務防線（Billing 預算與快訊）"
      contains: "Budgets & alerts"
  key_links: []
---

<objective>
把 `docs/DEPLOYMENT.md` 第 6 節從「GCP Gemini 每日配額（最後財務防線）」的 7 步配額操作，改寫為反映實測結論的「GCP Gemini 財務防線（Billing 預算與快訊）」。

Purpose: 今天在 GCP Console 實測發現本專案金鑰所屬 project 是付費層（Tier 1 / Postpay），付費層對一般純文字 GenerateContent 請求沒有可調整的每日請求配額（RPD）可設；舊第 6 節的「Edit quota 設 200 次/天」在此帳戶類型下實際找不到對應項目、操作不了。真正落地的財務防線改用 Billing → Budgets & alerts（每月預算＋快訊）。文件必須反映實測結果，並留下可重做的操作路徑，讓未來的人不用重新踩一次雷。

Output: 更新後的 `docs/DEPLOYMENT.md`（僅第 6 節內容變動，其餘章節不動）。
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@docs/DEPLOYMENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改寫 DEPLOYMENT.md 第 6 節為 Billing 預算與快訊</name>
  <files>docs/DEPLOYMENT.md</files>
  <action>
只編輯第 6 節（目前為第 67-79 行「## 6. GCP Gemini 每日配額（最後財務防線）」到第 79 行結尾），第 1-66 行與第 80 行以後（第 7、8 節等）保持原樣，不得改動。

將第 6 節整段替換為以下語意內容（維持既有 markdown 風格：`## 6. ...` 標題、繁中說明、必要處用有序清單）：

1) 標題改為 `## 6. GCP Gemini 財務防線（Billing 預算與快訊）`。

2) 開頭沿用原本「這是手動操作，用來封頂帳單」的框架，但把措辭調整為「每月帳單預算快訊」而非「單日配額」：說明這是純 GCP Console 手動操作、不是程式碼或本專案設定檔能重現的步驟，文件只描述操作路徑供未來人工重做。

3) 加一小段說明為何舊做法（設每日 RPD 配額）在本帳戶行不通，避免未來讀者重複踩雷：本專案金鑰所屬 project 為付費層（Tier 1 / Postpay）；在 Console 的「配額與系統限制」頁面篩選 per day / generatecontent 後，可見的每日配額全部是 free tier 專屬的 input token 數限制，或僅限有接 Google Search grounding / Map grounding 的呼叫才會計入；本專案 `services/gemini.ts` 是純文字分析呼叫、未使用 Search/Map grounding，因此不受這些 per day 配額影響——付費層對一般 GenerateContent 請求沒有可調整的每日請求配額（RPD）可設，故舊版「Edit quota 設約 200 次/天」在此帳戶類型下找不到對應項目、操作不了。

4) 用有序清單取代原 7 步配額走查，改寫為實際落地的 Billing 預算操作路徑：開啟 Google Cloud Console → Billing（帳單）→ 左側 Budgets & alerts（預算與快訊）→ Create budget（建立預算）→ Scope（範圍）鎖定 Gemini API key 所屬 project（本專案為 `chuan-483103`，避免涵蓋帳單帳戶下其他 project）→ Budget type 選 Specified amount（指定金額），設每月固定金額（本專案設約 $10 USD／月）→ 設定 alert thresholds 門檻 50% / 90% / 100% → 通知對象勾選 email 給帳單管理員（Billing account admins/users）。

5) 明確標註：不設定任何自動化斷線動作（不接 Pub/Sub 觸發關閉服務），純 email 快訊——理由是避免誤傷同 project／同帳單帳戶下其他非 Gemini 服務（例如 BigQuery、Compute Engine）。

6) 移除原本只適用於舊配額做法的「RPD 通常以太平洋時間午夜重置」整句；改為一句說明預算以「月」為週期計算與重置（非每日重置）。可保留「即使用 AI Studio 產生的 key 仍歸屬某個 Google Cloud project，帳單與預算在該 project／其帳單帳戶管理」這類仍然成立的提醒。

不要在文件中寫入任何實際金鑰或帳單帳戶祕密（章節只描述 Console 操作路徑）。除第 6 節外，不得改動 `docs/DEPLOYMENT.md` 任何其他章節。
  </action>
  <verify>
    <automated>bash -c 'set -e; f="docs/DEPLOYMENT.md"; grep -q "GCP Gemini 財務防線" "$f"; grep -q "Budgets & alerts" "$f"; grep -q "chuan-483103" "$f"; grep -q "50%" "$f"; test "$(grep -c "Edit quota" "$f")" = "0"; test "$(grep -c "太平洋時間午夜" "$f")" = "0"; test "$(grep -c "^## " "$f")" = "8"; echo OK'</automated>
  </verify>
  <done>第 6 節標題為「GCP Gemini 財務防線（Billing 預算與快訊）」；內含付費層無 RPD 的原因說明與 Billing → Budgets & alerts 完整操作路徑（project 範圍、每月固定金額、50/90/100% 門檻、email 通知、無自動化斷線）；舊的「Edit quota」7 步與「太平洋時間午夜」重置說明已移除；DEPLOYMENT.md 仍為 8 個 `## ` 頂層章節、其餘章節逐字不變。</done>
</task>

</tasks>

<verification>
- 全文仍為 8 個頂層章節（`## 1.` 到 `## 8.`），第 6 節以外章節逐字不變。
- 第 8 節「部署後驗收清單」不引用「第 6 節」或每日配額概念（原本即無），無需連動修改；驗證確認其內容未變。
- 文件不含任何實際 API 金鑰或帳單祕密。
</verification>

<success_criteria>
- `grep "GCP Gemini 財務防線" docs/DEPLOYMENT.md` 命中。
- `grep "Budgets & alerts" docs/DEPLOYMENT.md` 命中；`grep "chuan-483103"` 命中；`grep "50%"` 命中。
- `grep -c "Edit quota" docs/DEPLOYMENT.md` 為 0；`grep -c "太平洋時間午夜" docs/DEPLOYMENT.md` 為 0。
- `grep -c "^## " docs/DEPLOYMENT.md` 為 8（章節數不變）。
</success_criteria>

<output>
Create `.planning/quick/260711-0hf-docs-deployment-md-6-gcp-gemini-billing/260711-0hf-SUMMARY.md` when done
</output>
