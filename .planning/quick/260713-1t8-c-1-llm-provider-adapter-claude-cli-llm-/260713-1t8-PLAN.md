---
phase: quick-260713-1t8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/_lib/llm.ts
  - api/gemini.ts
  - .env.example
autonomous: true
requirements: [C1-LLM-ADAPTER]
must_haves:
  truths:
    - "未設 LLM_PROVIDER（或設 gemini-api）時，/api/gemini 的端點 URL、request/response 格式、錯誤 JSON（code+message）、HTTP status 與現狀完全相同——Vercel 部署行為零改變"
    - "LLM_PROVIDER=claude-cli 且本機 CLI 未登入時，回傳分類錯誤，message 明確指示「跑 claude /login（或 claude setup-token）後重試；或暫時移除 LLM_PROVIDER 改走 gemini-api」"
    - "claude-cli 路徑逾時（100s）或任何失敗路徑不遺留未終止子程序、不懸掛 promise（settled 旗標防重複 resolve）"
    - "LLM_PROVIDER 為其他值時回明確設定錯誤（不靜默 fallback）"
    - "GEMINI_API_KEY 邏輯零觸碰；claude-cli 僅在 env 顯式設定時啟用；無任何 OAuth token 進 repo"
  artifacts:
    - path: "api/_lib/llm.ts"
      provides: "provider adapter generateText＋claude-cli 橋接（執行檔探索/spawn/逾時/JSON 解析/錯誤分類）"
      exports: ["generateText"]
      min_lines: 100
    - path: "api/gemini.ts"
      provides: "handler 改呼叫 generateText，guard/限流/驗證/錯誤回應照舊"
      contains: "generateText"
    - path: ".env.example"
      provides: "LLM_PROVIDER / CLAUDE_CLI_PATH / CLAUDE_CLI_MODEL_* 說明＋一次性 claude /login 前置"
      contains: "LLM_PROVIDER"
  key_links:
    - from: "api/gemini.ts"
      to: "api/_lib/llm.js"
      via: "import { generateText }（ESM 必須帶 .js 副檔名——260710-17a 教訓）"
      pattern: "from './_lib/llm\\.js'"
    - from: "api/_lib/llm.ts gemini-api 分支"
      to: "api/_lib/http.ts callGeminiWithTimeout"
      via: "getGeminiApiKey + getModelForMode + callGeminiWithTimeout 既有路徑原樣搬移"
      pattern: "callGeminiWithTimeout"
    - from: "api/_lib/llm.ts claude-cli 分支"
      to: "node:child_process spawn"
      via: "spawn 完整執行檔路徑（不經 shell）＋stdin 寫 prompt＋清洗後 env＋cwd=os.tmpdir()"
      pattern: "spawn\\("
---

<objective>
C-1 後端 LLM provider adapter＋claude-cli 訂閱橋接（Phase C 子包 1/3，權威規格：.planning/optimization/PLAN.md「Phase C（P4A）LLM 訂閱橋接」章節前三條）。

新增 `api/_lib/llm.ts` 的 `generateText(req)`，依 `LLM_PROVIDER` env 分流：未設或 `gemini-api` → 既有 callGeminiWithTimeout 路徑；`claude-cli` → child_process 橋接本機 Claude Code CLI（Claude 訂閱、零 Gemini 帳單）；switch 預留 `codex-cli`/`gemini-cli` 註記擴充點（不實作）。`api/gemini.ts` handler 改呼叫 adapter，對前端 API 契約零改動。

Purpose: 本機 vercel dev 日常分析改吃 Claude 訂閱，Gemini 帳單趨近 0；adapter 抽象同時是 Anthropic headless 政策風險的保險。部署環境（Vercel 不設 LLM_PROVIDER）行為與今日逐位元相同。
Output: api/_lib/llm.ts 新檔＋api/gemini.ts 改接＋.env.example 文件＋scratchpad 直測記錄＋SUMMARY.md
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/optimization/PLAN.md（「Phase C（P4A）LLM 訂閱橋接」章節）
@api/gemini.ts
@api/_lib/http.ts
@api/_lib/config.ts
@.env.example

**Orchestrator 已實測的硬事實（勿重查、直接當前提）：**
- 本機 `claude` 不在 PATH。
- 執行檔在 `C:\Users\jason\AppData\Roaming\Claude\claude-code\2.1.205\claude.exe`（version 2.1.205）。
- `--help` 確認支援：`-p`、`--output-format json`、`--system-prompt`、`--tools`、`--no-session-persistence`、`--disable-slash-commands`、`--model`。
- 該 CLI 目前未登入：headless 回 `{"type":"result","subtype":"success","is_error":true,...,"result":"Not logged in · Please run /login"}`。
- `--bare` 永不讀 OAuth——**禁用**。

**既有程式碼事實（planner 已讀碼確認）：**
- `GeminiRequest` 型別、`ClassifiedError`、`GeminiErrorCode`、`callGeminiWithTimeout`、`sanitizeErrorForLog` 全在 `api/_lib/http.ts`。
- 前端 `services/gemini.ts:39-40` 只讀 `data.message`（非 ok 時），**不 switch `code`**——沿用既有錯誤碼＋自訂 message 對前端零影響。
- `@types/node@^22.14.0` 在 devDependencies、tsconfig `types:["node"]`——`node:child_process`/`node:os`/`node:fs`/`node:path` 匯入可直接過 tsc。
- api/ 下 ESM 相對匯入**必須帶 `.js` 副檔名**（260710-17a：Vercel production 原生 ESM 會 ERR_MODULE_NOT_FOUND）。
- esbuild 經 vite 傳遞安裝，`npx esbuild` 可用（本次零新增 npm 套件——只用 Node 內建模組）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 api/_lib/llm.ts——provider adapter＋claude-cli 橋接</name>
  <files>api/_lib/llm.ts</files>
  <action>
新檔 `api/_lib/llm.ts`，匯出 `generateText(req: GeminiRequest): Promise<{ text: string }>`。所有相對匯入帶 `.js` 副檔名（`./http.js`、`./config.js`）；Node 內建用 `node:` 前綴（`node:child_process`、`node:os`、`node:fs`、`node:path`）。

**分流 switch**（讀 `(process.env.LLM_PROVIDER ?? '').trim()`）：
- 空字串或 `gemini-api` → 既有路徑原樣搬移：`getGeminiApiKey()` 取金鑰，無金鑰 `throw new ClassifiedError('MISSING_KEY')`（errorMessages 預設訊息與現行 handler 硬編字串逐字相同，回應 JSON 不變）；有金鑰則組 `contents = [{role:'user',parts:[{text:prompt}]}]`、`config = { systemInstruction, temperature: temperature ?? 0.1, ...(thinkingConfig?{thinkingConfig}:{}) }`，呼叫 `callGeminiWithTimeout({apiKey, model: getModelForMode(mode), contents, config})`。**GEMINI_API_KEY 邏輯零觸碰（紅線）**。
- `claude-cli` → 下述橋接。
- 其他值 → `throw new ClassifiedError('MISSING_KEY', 'LLM_PROVIDER 設定值無效（支援 gemini-api、claude-cli），請修正環境變數。')`。**Planner 裁定：不加新 GeminiErrorCode**——前端只讀 message 不讀 code（services/gemini.ts:39-40 讀碼確認），沿用 MISSING_KEY（→500，語意＝伺服器端設定問題）即可，api/gemini.ts 的 statusByCode 零改動。
- switch 內留註解標記 `codex-cli`/`gemini-cli` 未來擴充點，**只留註記不實作**。

**claude-cli 橋接**（獨立函式，如 `callClaudeCli(req): Promise<{text:string}>`）：

1. **執行檔探索**（module 級變數快取結果）：(a) `CLAUDE_CLI_PATH` env 存在且 `fs.existsSync` → 用它；(b) 掃 `process.env.PATH` 各目錄找 `claude.exe`（win32；非 win32 找 `claude`）——**刻意跳過 .cmd shim**（Node spawn 不經 shell 無法執行 .cmd，而 shell:true 是引號地獄，禁用）；(c) 掃 `path.join(process.env.APPDATA ?? '', 'Claude', 'claude-code')` 子目錄，過濾名稱符合 `/^\d+(\.\d+)*$/` 且內含 `claude.exe` 者，按版本段逐位數值比較取最高（版本目錄隨 app 更新變動，不可硬編 2.1.205）。全部落空 → `throw new ClassifiedError('MISSING_KEY', '找不到 claude 執行檔：請設 CLAUDE_CLI_PATH 指向 claude.exe，或暫時移除 LLM_PROVIDER 改走 gemini-api。')`。

2. **模式對應**：fast→`sonnet`、thinking→`opus`；`CLAUDE_CLI_MODEL_FAST`/`CLAUDE_CLI_MODEL_THINKING` env 可覆寫。`temperature`/`thinkingConfig` 在此路徑**靜默丟棄**（CLI 不支援，勿報錯勿警告）。

3. **spawn**：直接執行完整路徑，**不經 shell**。args：`['-p', '--output-format', 'json', '--tools', '', '--no-session-persistence', '--disable-slash-commands', '--model', model, '--system-prompt', systemInstruction]`。prompt 走 stdin：spawn 後**立即** `child.stdin.write(prompt)` ＋ `child.stdin.end()`（CLI 對 piped stdin 3 秒無資料會發警告，立即寫入避開）。選項：`cwd: os.tmpdir()`（避免載入專案 hooks/CLAUDE.md/skills）、`env` 用 process.env 淺拷貝後**剔除** `ANTHROPIC_BASE_URL`、`CLAUDECODE`、所有 `CLAUDE_CODE_*` 開頭鍵（避免從 Claude Code 會話啟動的 vercel dev 讓子 CLI 繼承宿主閘道/遞迴旗標）、`windowsHide: true`。備註：若實作時確認 2.1.205 支援 `--system-prompt-file` 且 systemInstruction 過長（Windows CreateProcess 命令列 ~32K 上限），可改寫暫存檔傳路徑——此為 Claude 裁量，預設直傳字串。

4. **逾時與 promise 收斂**（Phase B H-1 教訓——逾時/失敗路徑不得遺留子程序或懸掛 promise）：包成單一 `new Promise`，用 `settled` 布林旗標守衛所有 resolve/reject 出口。`setTimeout(100_000)` 逾時 → 標記 settled、`child.kill()`、reject `new ClassifiedError('UPSTREAM_ERROR')`。任何出口（close/error/timeout）先檢查 `settled`，settle 時 `clearTimeout`。stdout/stderr 用 `data` 事件累積字串（設 `encoding('utf8')`）；`error` 事件（spawn 失敗）→ UPSTREAM_ERROR；`close` 事件做解析。

5. **輸出解析**（JSON 格式已實測確認）：`JSON.parse(stdout.trim())` 期望單一物件 `{type:"result", subtype, is_error, result}`。(a) `is_error===true` 且 `result` 含 `"Not logged in"` → `throw new ClassifiedError('MISSING_KEY', '本機 Claude CLI 未登入：請在終端跑 claude /login（或 claude setup-token）後重試；或暫時移除 LLM_PROVIDER 改走 gemini-api。')`；(b) 其他 `is_error===true` → UPSTREAM_ERROR，message 附 `sanitizeErrorForLog(String(result))` 截斷 ~200 字的摘要（此路徑只在本機出現，方便除錯）；(c) 解析失敗或 stdout 空 → UPSTREAM_ERROR（stderr 摘要進 message 同 (b) 處理）；(d) **exit code 非 0 但 stdout 有合法 result JSON → 以 JSON 為準**（先 parse 再看 exit code）；(e) 正常：`is_error===false` → resolve `{ text: String(json.result ?? '') }`。

**紅線**：`--bare` 禁用（永不讀 OAuth）；任何 OAuth token 不進 repo。
  </action>
  <verify>
    <automated>npx tsc --noEmit（零新增錯誤）；Bash: grep -c "\.js'" api/_lib/llm.ts ≥2（相對匯入帶副檔名）；grep -c "bare" api/_lib/llm.ts = 0</automated>
  </verify>
  <done>api/_lib/llm.ts 存在，匯出 generateText；三分支（預設/claude-cli/無效值）齊備；codex-cli/gemini-cli 僅註記；橋接含探索快取、env 清洗、settled 旗標逾時收斂、五種解析出口；tsc 過</done>
</task>

<task type="auto">
  <name>Task 2: api/gemini.ts 改接 adapter＋.env.example 文件</name>
  <files>api/gemini.ts, .env.example</files>
  <action>
**api/gemini.ts**：handler 改為 guards → method 檢查 → `validateGeminiRequest(req.body)` → `const result = await generateText(request)` → `res.status(200).json(result)`。刪除 handler 內的 `getGeminiApiKey`/apiKey 檢查/contents/config 組裝/`callGeminiWithTimeout` 呼叫（全數已搬入 adapter 的 gemini-api 分支），import 改為 `import { generateText } from './_lib/llm.js';`（保留 ClassifiedError/classifyGeminiError/sanitizeErrorForLog/GeminiErrorCode/validateGeminiRequest 的既有 import 與 catch 區塊、statusByCode、maxDuration=120——**全部零改動**）。

**契約等價自查**（寫進 SUMMARY 的證明）：端點 URL、request 驗證、成功回應 `{text}`、錯誤 JSON `{code,message}`、各 code 的 HTTP status 均不變。唯一已知 delta：無金鑰時 MISSING_KEY 原為 handler 直接 return，現改為 adapter throw 經 catch 收斂——回應 body/status 逐字相同（errorMessages['MISSING_KEY'] 與原硬編字串同一句），差異僅多一行 console.error 日誌（log-only，非契約）。

**.env.example**：檔尾新增區塊——`LLM_PROVIDER=`（註解：僅本機 vercel dev 用；可選值 gemini-api（預設，未設同義）｜claude-cli（走本機 Claude Code CLI，吃 Claude 訂閱，需先在終端跑一次 `claude /login`）；**Vercel 部署不設此變數 → 永遠 gemini-api**）、`CLAUDE_CLI_PATH=`（選填：claude.exe 完整路徑；未設時自動探索 PATH 與 %APPDATA%\Claude\claude-code 最高版本目錄）、`CLAUDE_CLI_MODEL_FAST=`／`CLAUDE_CLI_MODEL_THINKING=`（選填：預設 fast→sonnet、thinking→opus）。
  </action>
  <verify>
    <automated>npx tsc --noEmit；Bash: grep -c "callGeminiWithTimeout" api/gemini.ts = 0 且 grep -c "generateText" api/gemini.ts ≥1 且 grep -c "statusByCode" api/gemini.ts ≥2（catch 區塊未動）；grep -c "LLM_PROVIDER" .env.example ≥1</automated>
  </verify>
  <done>handler 只負責 guard/驗證/呼叫 adapter/錯誤映射；statusByCode 與 catch 零改動；.env.example 含四個新 env 與 claude /login 前置說明；tsc 過</done>
</task>

<task type="auto">
  <name>Task 3: 直測驗證——三情境實跑橋接函式（scratchpad，不進 repo）</name>
  <files>（無 repo 檔案——測試腳本放 scratchpad）</files>
  <action>
用 esbuild 把 adapter 打包成可直接 node 執行的 CJS（避免 tsx 新依賴；esbuild 經 vite 已在 node_modules）：

`npx esbuild "api/_lib/llm.ts" --bundle --platform=node --format=cjs --outfile="<scratchpad>/llm-bundle.cjs"`（@google/genai 一併打包，scratchpad 在專案外也能 require）。

寫 driver `<scratchpad>/llm-test.cjs`，require bundle 後依序跑三情境（用 Bash 工具執行 `node "<scratchpad>/llm-test.cjs"`）：

1. **預設路徑守衛**：`delete process.env.GEMINI_API_KEY; delete process.env.LLM_PROVIDER;` → `generateText({prompt:'hi', systemInstruction:'test', mode:'fast'})` 預期 reject，`err.code==='MISSING_KEY'` 且 message 含「Gemini API 金鑰」（證明未設 LLM_PROVIDER 走 gemini 分支、金鑰守衛先於任何網路呼叫）。
2. **無效值**：`process.env.LLM_PROVIDER='bogus'` → 預期 reject，message 含 `LLM_PROVIDER`。
3. **claude-cli 未登入 e2e**（本包在使用者登入前能做到的最深驗證）：`process.env.LLM_PROVIDER='claude-cli'` → 同樣呼叫，預期：(a) 探索命中 `%APPDATA%\Claude\claude-code\<最高版本>\claude.exe`（駕駛可 log 探索結果確認非硬編）；(b) promise 在遠小於 100s 內 settle；(c) reject 的 `err.code==='MISSING_KEY'` 且 message 含 `claude /login`；(d) **殘留偵測**：三情境斷言全過後印 PASS，再掛一支 `setTimeout(15s).unref()` 的守衛 timer——若 node 程序 15 秒後仍活著（洩漏的子程序 stdio/timer 撐住 event loop）timer 觸發印 FAIL 並 `process.exit(1)`；乾淨路徑 node 自然退出、timer 不礙事。

**gemini-api 等價讀碼證明**：對 api/gemini.ts 跑 `git diff`，逐段確認搬移前後語序等價（guards→method→validate→模型/config 組裝→呼叫→200/catch），把對照結論寫進 SUMMARY（含 MISSING_KEY log-only delta 的記載）。

**收尾**：SUMMARY.md 記載 LLM_PROVIDER/CLAUDE_CLI_PATH/CLAUDE_CLI_MODEL_* 用法、一次性 `claude /login` 前置、探索順序、已知限制（systemInstruction 走命令列 ~32K 上限、temperature/thinkingConfig 在 claude-cli 路徑忽略）。測試腳本與 bundle 留在 scratchpad，**不進 repo、不 commit**。最後 `npm run build` 後 Bash 跑 `grep -r "AIza" dist/` 必須無結果（金鑰紅線例行驗證）。
  </action>
  <verify>
    <automated>node "<scratchpad>/llm-test.cjs" 印出三情境 PASS 且程序 15 秒內自然退出（exit 0）；npx tsc --noEmit；npm run build && grep -r "AIza" dist/ 無結果（Bash 工具）</automated>
  </verify>
  <done>三情境斷言全過：預設路徑 MISSING_KEY 守衛、無效值明確錯誤、claude-cli 未登入分類錯誤＋訊息正確＋無殘留子程序/懸掛 handle；AIza 掃描乾淨；SUMMARY 記載 env 用法與等價證明</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/gemini | 不受信 request body（既有 validateGeminiRequest＋guard/限流，本包零改動） |
| adapter→child process | prompt/systemInstruction 進入子程序 argv/stdin（新邊界） |
| host env→child env | 宿主 Claude Code 會話變數可能滲入子 CLI（新邊界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C1-01 | Tampering（命令注入） | callClaudeCli spawn | mitigate | spawn 完整路徑＋args 陣列、不經 shell；prompt 走 stdin 不進 argv |
| T-C1-02 | Information Disclosure（金鑰/token 洩漏） | 錯誤訊息與日誌 | mitigate | 沿用 sanitizeErrorForLog；GEMINI_API_KEY 邏輯零觸碰；build 後 grep AIza 驗證 |
| T-C1-03 | Elevation（子 CLI 繼承宿主閘道/工具） | child env | mitigate | env 剔除 ANTHROPIC_BASE_URL/CLAUDECODE/CLAUDE_CODE_*；`--tools ""`＋--no-session-persistence＋--disable-slash-commands；cwd=os.tmpdir() 避開專案 hooks/skills |
| T-C1-04 | DoS（殭屍子程序堆積） | 逾時/失敗路徑 | mitigate | 100s 逾時 child.kill()＋settled 旗標；直測腳本 15s 殘留偵測 |
| T-C1-05 | Spoofing（部署環境誤走 claude-cli） | LLM_PROVIDER 分流 | mitigate | 僅顯式 `LLM_PROVIDER=claude-cli` 啟用；Vercel 不設此 env → 預設分支＝今日行為；無效值報錯不 fallback |
| T-C1-SC | Tampering | npm 供應鏈 | accept | 本包零新增套件（僅 Node 內建 child_process/os/fs/path），無安裝面 |
</threat_model>

<verification>
1. `npx tsc --noEmit` 全綠（零新增錯誤）。
2. gemini-api 等價：git diff 讀碼證明 handler 搬移語序等價；唯一 delta 為 MISSING_KEY 的 log-only 差異（回應 body/status 逐字相同）。
3. claude-cli e2e（登入前上限）：直測腳本三情境 PASS＋程序自然退出無殘留。
4. `npm run build` 後 `grep -r "AIza" dist/` 無結果。
5. 測試腳本只存在於 scratchpad，git status 不含任何測試產物。
</verification>

<success_criteria>
- 部署環境（無 LLM_PROVIDER）/api/gemini 契約與今日逐位元相同（status、JSON 欄位、訊息字串）。
- 本機設 `LLM_PROVIDER=claude-cli` 後：CLI 未登入得到明確導引訊息；使用者跑過 `claude /login` 後即可零 Gemini 帳單分析（登入後驗證屬使用者後續動作，不阻塞本包）。
- 失敗/逾時路徑零殭屍子程序、零懸掛 promise。
- GEMINI_API_KEY 紅線、--bare 禁用、OAuth 不進 repo 全數守住。
</success_criteria>

<output>
完成後建立 `.planning/quick/260713-1t8-c-1-llm-provider-adapter-claude-cli-llm-/260713-1t8-SUMMARY.md`
</output>
