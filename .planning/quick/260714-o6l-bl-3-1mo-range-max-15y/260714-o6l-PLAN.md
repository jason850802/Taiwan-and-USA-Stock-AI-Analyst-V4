---
quick_id: 260714-o6l
description: "BL-3 1mo range收斂max到15y加載入骨架屏"
source_spec: .planning/optimization/BL-PLAN.md §BL-3
status: ready
files_modified:
  - api/_lib/yahoo.ts
  - services/yahoo.ts
  - App.tsx
must_haves:
  truths:
    - "1mo 抓 range=15y（~180 根月棒），payload 較 max 明顯下降；1wk 維持 5y 不動"
    - "切週期／換標的載入時顯示 K 線骨架 shimmer＋「載入 K 線中…」，不再 blur 舊週期的圖誤導"
    - "容器高度不跳動、無白屏；快取命中的切回不出現骨架屏（loading 不觸發，既有行為）"
    - "月線最近端指標值與改前一致（warm-up 邊際：MA60 剩 120 根有效值、EMA 種子差衰減 ≈ 0）"
  artifacts:
    - "api/_lib/yahoo.ts: '1mo' 白名單含 '15y'（保留 'max' 供部署過渡期舊 bundle）"
    - "services/yahoo.ts: 1mo mainRange='15y'"
    - "App.tsx: 載入覆蓋層改骨架屏"
---

# Quick Task 260714-o6l：BL-3 1mo range 收斂（max→15y）＋載入骨架屏

**規格來源**：`.planning/optimization/BL-PLAN.md` §BL-3。錨點已依 2026-07-14 現行碼（BL-1/BL-2 落地後）核實。

**已核實錨點**：
- `api/_lib/yahoo.ts:27-33`：`INTERVAL_RANGE_MAP`，現為 `'1mo': ['max']`（BL-1 已把 1d 加 '2y'，同一張表）。**BL-PLAN drift 同款**：不加 '15y' 會被 validateChartParams 400 擋下；**保留 'max'** 讓部署過渡期（CDN 上的舊 bundle）不被 400，記 SUMMARY。
- `services/yahoo.ts`（fetchStockDataUncached 開頭 mainRange if-chain）：`else if (interval === '1mo') mainRange = 'max';`——改 `'15y'`。`1wk` 的 `5y` 不動。
- `App.tsx:445-452`：載入覆蓋層——`loading && <div className="absolute inset-0 z-20 ... bg-slate-900/60 backdrop-blur-sm"><Loader2 spin/><span>載入中…</span></div>`，蓋在 `<div className="relative">`（:440）內的 StockChart 上。

**指標安全邊際（BL-PLAN 已算好，不重算）**：15y ≈ 180 根月棒；MA60 warm-up 後仍有 ~120 根有效值；MACD(10,20,10) 約 30-45 根後穩定；EMA 種子差異在最近端 ≈ (1-2/21)^160 ≈ 0。

---

## Task 1：1mo range 收斂

**Files**: `api/_lib/yahoo.ts`, `services/yahoo.ts`

**Action**:
1. `api/_lib/yahoo.ts`：`'1mo': ['max']` → `'1mo': ['max', '15y']`，註解補一句（15y 為前端現行請求值；max 保留供部署過渡期舊 bundle）。
2. `services/yahoo.ts`：`else if (interval === '1mo') mainRange = 'max';` → `mainRange = '15y';`（附一行註解：BL-3 收斂——max 對 2330/AAPL 拉 380-550 根月棒，絕大多數在預設視窗外）。

**Verify**: `npx tsc --noEmit`。

**Commit**: `perf(bl-3): 1mo range 收斂 max→15y（後端白名單加 15y、保留 max 供過渡）`

## Task 2：載入覆蓋層改 K 線骨架屏

**Files**: `App.tsx`

**Action**:
1. `:440` 的 `<div className="relative">` 加 `min-h-[420px]`（首載 data=[] 時容器不塌陷、無白屏；有舊圖時高度由 StockChart 撐、min-h 不起作用）。
2. `:447-452` 覆蓋層改骨架屏（視覺細節裁量，硬要求：不再 blur 透出舊週期的圖、容器高度不跳動）：
   - 外層：`absolute inset-0 z-20 flex flex-col items-center justify-center gap-3 bg-surface`（不透明底、遮住舊圖；沿用 slate 深色主題）。
   - K 線骨架：一排 8-12 根垂直棒（隨機/交錯高度 40-80%，`bg-slate-700` 圓角），用 Tailwind 內建 `animate-pulse`＋每根 inline `style={{ animationDelay }}` 交錯產生 shimmer 流動感；不新增 CSS keyframes、不改 tailwind.config。
   - 文案：`載入 K 線中…`（`text-slate-400 text-sm`）。
   - 保留 `loading &&` 條件與 z-20 註解語意（不覆蓋 ChartToolbar）；快取命中切回 loading 不觸發 → 骨架屏自然不出現（零額外邏輯）。
3. Loader2 若 App.tsx 他處不再使用則從 import 移除；仍有使用就保留（檢查後決定，不留死 import）。

**Verify**: `npx tsc --noEmit`；grep 確認 `backdrop-blur` 已從該覆蓋層移除。

**Commit**: `feat(bl-3): 切週期載入覆蓋層改 K 線骨架 shimmer——不再 blur 舊週期誤導`

---

## 明確不做（BL-PLAN 拍板）

- 週/月線兩段式載入（range 收斂＋BL-2 並行後單段預期已夠快；BL-4b 後測仍 >5s 才另立條目）。
- 不動 1wk range、不動快取層、不動 StockChart。

## 驗收（本包內）

- `npx tsc --noEmit` 兩 commit 各自綠。
- payload 對比（2330/AAPL 1mo 改前後 size）、月線左移 ~15 年、最近端 MA60/MACD 一致（<0.01%）、骨架屏視覺／高度不跳——留統測（preview 3001）實測。
