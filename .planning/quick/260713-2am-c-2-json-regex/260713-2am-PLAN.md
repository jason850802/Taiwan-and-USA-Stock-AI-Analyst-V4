---
phase: quick-260713-2am
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/_shared/healthDecision.ts
  - services/gemini.ts
  - components/Portfolio.tsx
autonomous: true
requirements: [C-2]
must_haves:
  truths:
    - "單檔健檢的決策改由報告末尾 json 圍欄區塊解析取得；json 缺失/損壞時 fallback 既有 regex，行為永不比今天差"
    - "健檢 Modal 顯示的 markdown 在 json 解析成功時不含 json 圍欄區塊（機器區不給人看）"
    - "庫存 header 有「全部健檢」按鈕：一次 analyzePortfolioHealth 呼叫涵蓋全部持股，逐檔顯示決策 Badge 與各自報告"
    - "批次報告按「### 📋 持股健檢報告：」標頭切段各給各的；📊 庫存總覽／💡 整體操作建議段附在每檔段尾不遺失；切段失敗時整份全文塞給每檔"
    - "批次資料準備 getStockData 併發上限 3，不會無上限轟 Yahoo/FinMind"
    - "單檔健檢按鈕行為照舊；批次進行中各列自然顯示 loading spinner"
  artifacts:
    - path: "services/_shared/healthDecision.ts"
      provides: "零依賴純函式：parseHealthDecisions / extractDecisionByRegex / splitHealthReport / DECISION_EMOJI"
      min_lines: 60
    - path: "services/gemini.ts"
      provides: "analyzePortfolioHealth systemInstruction 末尾新增機器可讀決策區契約段"
      contains: "機器可讀決策區"
    - path: "components/Portfolio.tsx"
      provides: "buildHealthItem 共用 helper、handleBatchHealthCheck、全部健檢按鈕、單檔改接解析器"
  key_links:
    - from: "components/Portfolio.tsx"
      to: "services/_shared/healthDecision.ts"
      via: "import parseHealthDecisions/extractDecisionByRegex/splitHealthReport/DECISION_EMOJI"
      pattern: "from '\\.\\./services/_shared/healthDecision'"
    - from: "services/gemini.ts systemInstruction"
      to: "parser 契約"
      via: "json 圍欄 decisions 陣列"
      pattern: "decisions"
---

<objective>
C-2（Phase C 2/3）：健檢決策 JSON 結構化＋一鍵批次健檢。

1. `analyzePortfolioHealth` 的 systemInstruction 新增「機器可讀決策區」契約：整份 markdown 報告最末尾輸出一個 ```json 圍欄區塊 `{"decisions":[{"symbol":"...","decision":"加碼|續抱|減碼|停利|停損"}]}`，替換 Portfolio.tsx:965 的脆弱 regex（regex 降級為 fallback 下限）。
2. 新增「全部健檢」：全部持股合併成一次 LLM 呼叫（A3 帳單瘦身當時明記延到 Phase C 的批次健檢在此完成），回應按報告標頭切段逐檔顯示。

Purpose: 決策解析從「對人讀 markdown 撈字」改為「模型輸出機器區＋前端穩健解析」，同時 N 檔持股由 N 次呼叫降為 1 次呼叫（省計費）。契約 provider 無關——對 gemini-api 與 C-1 已上線的 claude-cli adapter 都成立，不依賴任一供應商的 structured output 功能（勿用 Gemini responseSchema）。

Output: `services/_shared/healthDecision.ts`（新檔）＋ `services/gemini.ts`（systemInstruction 契約段）＋ `components/Portfolio.tsx`（buildHealthItem／單檔改接／批次健檢）。
</objective>

<context>

@services/gemini.ts（callGeminiApi 快取咽喉點 :19-49、PortfolioHealthItem :505、formatHealthCheckData :516、analyzePortfolioHealth :627-821）
@components/Portfolio.tsx（handleSingleHealthCheck :922-972、healthResults state :720、header 按鈕區 :1022-1040、健檢 Modal :1382-1399）
@services/_shared/geminiCache.ts（零 import 純模組先例——esbuild 轉 CJS 後 node 直測的模式照抄）

**既有事實（executor 心裡要有數，不要重查）：**

- **A3 透明快取互動**：`callGeminiApi` 以 `hash(systemInstruction + ' ' + prompt)` 為 key（geminiCache.ts buildCacheKey）。本次 systemInstruction 變更會讓舊快取自然 miss——正確行為，無需遷移、**不要動 geminiCache.ts**。批次與單檔是不同 prompt＝不同 key，互不污染；同日重按批次 0 次 API。
- **行情快取行為**（services/yahoo.ts :880-930 已讀碼確認）：`getStockData` 有 quoteCache（fresh 命中 0 網路請求、stale 走 SWR），但 `getLatestPrice`（fetchAllPrices 用的那支）**繞過 quoteCache 直打 fetchRawData**——所以 fetchAllPrices 並不會幫 getStockData 暖快取。使用者近期看過的標的會命中 quoteCache，沒看過的就是冷抓。結論：批次資料準備必須限制併發（上限 3），Yahoo/FinMind 429 是常態。
- **輸入標頭格式**（formatHealthCheckData :601）：`========== 持股 N：名稱（代號）==========`（全形括號）。JSON 契約的 symbol 要求逐字照抄此括號內代號，故與 `PortfolioHealthItem.symbol`（＝Portfolio items 的 symbol 字串）一致，可直接當 map key。
- **現行單檔決策 regex**（Portfolio.tsx :965）：`/操作決策[：:]\s*[【\[]?\s*(🟢\s*加碼|🔵\s*續抱|🟡\s*減碼|🟠\s*停利|🔴\s*停損)/`，取 match[1] 後 `\s+` 壓縮——這條搬進新模組當 fallback。
- **決策顯示慣例**：healthResults.decision 存「emoji＋素值」（如 `🟢加碼`）；Badge 顯示時自行剝 emoji（:278），decVariant 以 `includes('停損')` 等判色（:267-272）。JSON 給素值時前端映射 `{加碼:🟢, 續抱:🔵, 減碼:🟡, 停利:🟠, 停損:🔴}` 補回前綴。
- **UI 元件**：`Button` 支援 `disabled` prop（Button.tsx :6，含 disabled:opacity-50 樣式）；`HeartPulse`、`Loader2` 已在 Portfolio.tsx :6 import。
- **批次進行中單檔按鈕自然抑制**：per-symbol healthResults 設為 loading 後，表格該格渲染 spinner 而非健檢按鈕（:266），無需額外 disabled 接線。

**Planner 裁定（任務簡報授權裁量處）：**

1. **總覽段去處**：📊 庫存總覽＋💡 整體操作建議段**附在每檔段尾**（以 `\n\n---\n\n` 分隔）。理由：零新增 UI 面（不加總覽 modal/state），透過既有個股健檢 Modal 保證有顯示路徑，Portfolio.tsx 改動最小。
2. **輸出長度風險**：**不做 chunk 分批**，記為已知限制寫進 SUMMARY。理由：個人庫存規模 <10 檔、systemInstruction 已有「防 Token 溢出」精簡要求；若日後截斷實際發生，解析器回 null → fallback 全文＋regex，行為可退化不會壞。
3. **fallback regex 位置**：搬進新模組（`extractDecisionByRegex`），Portfolio 原地那條刪除改 import。理由：批次切段後需對每段重跑 regex，放模組可 node 直測。
4. **併發策略**：3-worker index 游標池（JS 單執行緒下 `const idx = cursor++` 在同步段內取號，安全）。

**紅線**：不動 `api/`（C-1 剛落地）；不動 `analyzeEntryWithGemini`／`analyzeTradeDecision`／`analyzeFundamentals` 行為；不動 geminiCache.ts；型別相容；`GEMINI_API_KEY` 金鑰紅線照舊。
</context>

<tasks>

<task type="auto">
  <name>Task 1: healthDecision 解析器純模組＋systemInstruction 機器可讀決策區契約</name>
  <files>services/_shared/healthDecision.ts, services/gemini.ts</files>
  <action>
**A. 新增 `services/_shared/healthDecision.ts`**（零 import 純模組，檔頭註明「須可用 esbuild 轉 CJS 後在 node 直測」，照 geminiCache.ts 先例；不得觸碰任何瀏覽器全域）。匯出：

1. `export const HEALTH_DECISIONS = ['加碼', '續抱', '減碼', '停利', '停損'] as const;`、`export type HealthDecision = typeof HEALTH_DECISIONS[number];`、`export const DECISION_EMOJI: Record<HealthDecision, string> = { 加碼: '🟢', 續抱: '🔵', 減碼: '🟡', 停利: '🟠', 停損: '🔴' };`

2. `export interface HealthDecisionEntry { symbol: string; decision: HealthDecision; }`、`export interface ParsedHealthDecisions { decisions: HealthDecisionEntry[]; cleanedMarkdown: string; }`

3. `export function parseHealthDecisions(fullText: string): ParsedHealthDecisions | null`——
   - 以 `/```json\s*([\s\S]*?)```/g` 全域掃描，取**最後一個**匹配（報告本體可能含其他 json 例示區塊，契約規定機器區在最末尾）。
   - 對捕獲組 `JSON.parse`（try/catch）。驗證 shape：物件、`decisions` 為非空陣列、每筆 `symbol` 為非空字串、每筆 `decision`（trim 後）屬 HEALTH_DECISIONS 五值枚舉。
   - `cleanedMarkdown` ＝ 原文剔除該最後一個 json 圍欄區塊（用匹配的 index＋length slice 拼接）後 `trimEnd()`。
   - 任何一步失敗（無匹配／parse 拋錯／shape 不符／枚舉外值）→ 回傳 `null`，不拋錯。

4. `export function extractDecisionByRegex(markdown: string): string | null`——逐字搬 Portfolio.tsx:965 現行 regex：`/操作決策[：:]\s*[【\[]?\s*(🟢\s*加碼|🔵\s*續抱|🟡\s*減碼|🟠\s*停利|🔴\s*停損)/`，命中回 `match[1].replace(/\s+/g, '')`（含 emoji 前綴，如 `🟢加碼`），未命中回 `null`。

5. `export interface SplitHealthReport { perSymbol: Record<string, string>; overview: string; }`、`export function splitHealthReport(markdown: string, symbols: string[]): SplitHealthReport | null`——
   - 以 `/^###\s*📋?\s*持股健檢報告[：:].*$/gm`（emoji 設為可選，防模型漏 emoji）收集標頭列起點；0 個標頭 → null。
   - 每段＝標頭起點到下一標頭起點（末段到文末）。
   - 總覽切割：在**最後一段**內搜尋 `/^###\s*(?:📊|💡)/m`；找到則該段在此截止、其後全部為 `overview`；找不到 `overview = ''`。
   - symbol→段映射：symbols 依字串長度**降冪**逐一認領（防 `6488.TW` 是 `6488.TWO` 子字串類邊角）——標頭列文字 `includes(symbol)` 且該段未被認領者恰 1 段 → 認領；0 段或歧義（>1 未認領段命中）→ 整體回 `null`。每個 symbol 都須認領到段，否則 `null`。
   - 成功回 `{ perSymbol（各段 trim）, overview（trim） }`。

**B. 修改 `services/gemini.ts` analyzePortfolioHealth 的 systemInstruction**：在「## 嚴格執行規範」清單第 4 點之後（模板字面值收尾反引號之前，約 :809 之後）追加一節：

```
## 機器可讀決策區（最末尾，嚴格遵守）
完成上述整份報告（含庫存總覽與整體操作建議）後，在回應的最末尾輸出恰好一個 json 圍欄區塊，格式：
\`\`\`json
{"decisions":[{"symbol":"2330.TW","decision":"續抱"}]}
\`\`\`
- decisions：每一檔持股恰好一筆，順序同輸入。
- symbol：逐字照抄輸入資料「持股 N：名稱（代號）」標頭全形括號內的代號（如 2330.TW、AAPL），不得增刪改寫。
- decision：只能是 加碼、續抱、減碼、停利、停損 五個值之一（純文字，不含 emoji、不含【】）。
- 此 json 區塊之後不得再輸出任何文字。
```

**陷阱警告**：systemInstruction 是 TS 模板字面值——上面那段裡的 ```json 圍欄反引號在 TS 原始碼中必須寫成 `\`\`\``（反斜線轉義），否則模板字面值提前終止、整檔語法爆炸。除追加此節外，systemInstruction 其他內容一字不動（診斷輸出格式、規則庫全保留——報告本體仍是人讀 markdown，經 react-markdown 渲染）。不動 callGeminiApi、不動其他 analyze* 函式。

**C. node 直測**（腳本放 scratchpad，不進 repo）：`npx esbuild services/_shared/healthDecision.ts --bundle --format=cjs --outfile=<scratchpad>/healthDecision.cjs` 後以 node 腳本斷言至少以下情境：
- 正常單檔：報告尾帶合法 json 區塊 → decisions 正確、cleanedMarkdown 不含 ```json、trimEnd 生效。
- 正常多檔（3 檔）：decisions 3 筆各對各。
- 缺 json 區塊 → null。
- 壞 JSON（截斷/多餘逗號）→ null。
- 枚舉外值（如 "觀望"、"🟢加碼" 帶 emoji）→ null。
- decisions 為空陣列 → null。
- 報告含多個 json 區塊 → 取最後一個。
- extractDecisionByRegex：對現行單檔輸出樣式（`**操作決策：【 🟢加碼 】**`）命中 `🟢加碼`；無決策文本回 null。
- splitHealthReport：3 檔正常切段（perSymbol 各含自己標頭）＋overview 含「庫存總覽」；標頭缺 1 檔 → null；`6488.TWO` 與長度排序認領正確；0 標頭 → null。
  </action>
  <verify>
    <automated>cd "E:\My Project\Taiwan-and-USA-Stock-AI-Analyst-V4" && npx tsc --noEmit && npx esbuild services/_shared/healthDecision.ts --bundle --format=cjs --outfile="$SCRATCHPAD/healthDecision.cjs" && node "$SCRATCHPAD/test-healthDecision.cjs"</automated>
  </verify>
  <done>healthDecision.ts 存在且零 import；node 直測全部情境 PASS；gemini.ts systemInstruction 末尾含「機器可讀決策區」節且模板字面值反引號正確轉義；tsc 綠。</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio.tsx 改接——buildHealthItem 共用 helper＋單檔改接解析器＋一鍵批次健檢</name>
  <files>components/Portfolio.tsx</files>
  <action>
Portfolio.tsx 1400+ 行——**edits 精準錨定，勿大段重寫**。改動點全部集中在主元件區（:705-1041）與 import 區（:1-12）：

**A. import**（:4 附近）：新增 `import { parseHealthDecisions, extractDecisionByRegex, splitHealthReport, DECISION_EMOJI } from '../services/_shared/healthDecision';`

**B. 抽 `buildHealthItem` 共用 helper**（消除單檔/批次複製）：在 handleSingleHealthCheck 上方新增 `useCallback`：

`const buildHealthItem = useCallback(async (symbol: string): Promise<PortfolioHealthItem | null> => { ... }, [items, prices, usdTwdRate]);`

函式體＝現 handleSingleHealthCheck :923-959 的成本/損益換算與資料抓取邏輯**逐字搬移**（lots 過濾→空回 null；currentPrice 取 prices；isUS/rate/totalShares/totalCostInCurrentCurrency/avgCostPriceInCurrentCurrency/profitPct 換算原封不動；try/catch 包 `getStockData(symbol,'1d')`＋`estimateVolumeTrend` 失敗仍繼續；組出 PortfolioHealthItem 回傳）。

**C. handleSingleHealthCheck 改寫**（:922-972 原函式）：
1. 開頭 guard：`if (!items.some(i => i.symbol === symbol)) return;`（保留「無 lots 不設 loading」的現行語意）。
2. 設 loading（照舊）→ `const healthItem = await buildHealthItem(symbol); if (!healthItem) return;` → `const result = await analyzePortfolioHealth([healthItem]);`
3. 決策與顯示文本改接解析器（**fallback 階梯，舊行為是下限**）：
   - `const parsed = parseHealthDecisions(result);`
   - `const entry = parsed?.decisions.find(d => d.symbol === symbol) ?? parsed?.decisions[0] ?? null;`
   - `const decision = entry ? DECISION_EMOJI[entry.decision] + entry.decision : (extractDecisionByRegex(result) ?? '分析完成');`
   - `const fullResult = parsed ? parsed.cleanedMarkdown : result;`
4. 原 :965 regex 那兩行刪除（已搬進模組）。setHealthResults done/error 寫法與 catch 分支照舊。

**D. 批次健檢**：
1. state：`const [batchChecking, setBatchChecking] = useState(false);`（放 :721 healthModalSymbol 附近）。
2. handler `handleBatchHealthCheck = useCallback(async () => {...}, [items, batchChecking, buildHealthItem])`：
   - `const symbols = Array.from(new Set(items.map(i => i.symbol)));` 空或 batchChecking 中直接 return。
   - `setBatchChecking(true)`；一次 setHealthResults 把全部 symbols 設 `{status:'loading', decision:'', fullResult:''}`。
   - **併發上限 3 的 index 游標池**組 items：`let cursor = 0;` ＋ `Array.from({length: Math.min(3, symbols.length)}, async () => { while (true) { const idx = cursor++; if (idx >= symbols.length) break; const it = await buildHealthItem(symbols[idx]); if (it) results[idx] = it; } })` → `await Promise.all(workers)` → filter 掉空洞得 healthItems（理由：getLatestPrice 不暖 getStockData 快取，冷抓可能真打網路，429 是常態不可無上限 Promise.all）。
   - `const result = await analyzePortfolioHealth(healthItems);`（一次呼叫；systemInstruction 的多檔庫存總覽段本來就支援多檔）。
   - 回應處置（fallback 階梯明確化）：
     a. `const parsed = parseHealthDecisions(result);` `const displayText = parsed ? parsed.cleanedMarkdown : result;`
     b. `const split = splitHealthReport(displayText, symbols);`
     c. `const decisionMap = new Map((parsed?.decisions ?? []).map(d => [d.symbol, d.decision]));`
     d. 一次 setHealthResults：對每個 symbol——`fullResult` ＝ split 成功時 `split.perSymbol[symbol] + (split.overview ? '\n\n---\n\n' + split.overview : '')`（總覽段附每檔段尾，planner 裁定）、split 失敗時 `displayText` 整份全文；`decision` ＝ decisionMap 有值時 `DECISION_EMOJI[d] + d`、否則 `extractDecisionByRegex(split ? split.perSymbol[symbol] : displayText) ?? '分析完成'`；status 'done'。
   - catch：全部 symbols 設 `{status:'error', decision:'分析失敗', fullResult:'**庫存健檢分析失敗**\n\n請稍後再試。'}`（沿用單檔 error 文案）。
   - finally：`setBatchChecking(false)`。
3. header 按鈕（:1031-1033「更新報價」按鈕之後插入）：
   `<Button variant="ai" onClick={handleBatchHealthCheck} disabled={items.length === 0 || batchChecking} className="flex items-center gap-2">{batchChecking ? <Loader2 size={15} className="animate-spin" /> : <HeartPulse size={15} />} 全部健檢</Button>`
   （HeartPulse/Loader2 已 import；Button 支援 disabled。）

**不改**：TwGroupTable/UsGroupTable 的 onHealthCheck 接線與三態渲染（loading spinner/decision Badge/error 全沿用——批次進行中 per-symbol loading 自然讓表格顯示 spinner、抑制單檔按鈕）；健檢 Modal :1382-1399 照舊（fullResult 已是切段後文本）；單檔健檢按鈕行為照舊。
  </action>
  <verify>
    <automated>cd "E:\My Project\Taiwan-and-USA-Stock-AI-Analyst-V4" && npx tsc --noEmit && grep -c "buildHealthItem\|handleBatchHealthCheck\|parseHealthDecisions\|splitHealthReport" components/Portfolio.tsx</automated>
  </verify>
  <done>tsc 綠；Portfolio.tsx 中：舊 :965 regex 字面已不存在（grep `操作決策\[：:\]` 無結果）、buildHealthItem 被單檔與批次兩處呼叫、全部健檢按鈕存在且 disabled 條件正確；成本/損益換算邏輯與原單檔逐字一致（讀碼斷言）。</done>
</task>

<task type="auto">
  <name>Task 3: 終端驗證——建置、金鑰紅線、迴歸讀碼斷言</name>
  <files>（驗證，無新改動）</files>
  <action>
1. `npm run build` 成功後 `grep -r "AIza" dist/` 必須無結果（金鑰紅線；用 Bash 工具跑，PowerShell 5.1 沒有 grep）。
2. 迴歸讀碼斷言（紅線核對）：
   - `git diff --stat` 只含三檔：services/_shared/healthDecision.ts（新）、services/gemini.ts、components/Portfolio.tsx——`api/` 零觸碰（C-1 剛落地）。
   - services/gemini.ts diff 只落在 analyzePortfolioHealth 的 systemInstruction（追加一節）；analyzeEntryWithGemini／analyzeTradeDecision／analyzeFundamentals／callGeminiApi／FLASH_THINKING_BUDGET 零改動。
   - services/_shared/geminiCache.ts 零改動（A3 快取以 hash(systemInstruction+prompt) 為 key，本次 systemInstruction 變更＝舊快取自然 miss，正確行為無需遷移）。
   - PortfolioHealthItem 介面零改動（型別相容紅線）。
3. SUMMARY 記錄已知限制：多檔合併輸出變長，>8 檔可能截斷（未 chunk——個人庫存 <10 檔、systemInstruction 已有防溢出精簡要求；截斷時解析器回 null → fallback 全文＋regex，可退化不會壞）。
  </action>
  <verify>
    <automated>cd "E:\My Project\Taiwan-and-USA-Stock-AI-Analyst-V4" && npm run build && grep -r "AIza" dist/ | wc -l | grep -q "^0$" && git diff --stat</automated>
  </verify>
  <done>build 成功；dist/ 無 AIza；diff 範圍恰為三檔；紅線斷言全過。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 回應 → 前端解析 | 模型輸出視為半信任輸入：json 區塊可能缺失、損壞、含枚舉外值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C2-01 | Tampering | parseHealthDecisions | mitigate | JSON.parse 包 try/catch＋shape 驗證＋五值枚舉白名單；任何失敗回 null 走 regex fallback；絕無 eval |
| T-C2-02 | DoS | handleBatchHealthCheck 資料準備 | mitigate | getStockData 併發上限 3，防自我 DoS 觸發 Yahoo/FinMind 429 |
| T-C2-03 | Info Disclosure | 金鑰紅線 | mitigate | 不新增套件、不動 api/；build 後 grep AIza dist/ 無結果 |
</threat_model>

<verification>
- `npx tsc --noEmit` 綠（每個 task 各跑一次）。
- 解析器 node 直測（esbuild CJS）全情境 PASS：正常單檔/多檔、缺區塊、壞 JSON、枚舉外值、空 decisions、多 json 區塊取最後、regex fallback、切段成功/失敗/子字串認領。
- `npm run build` ＋ `grep -r "AIza" dist/` 無結果。
- 讀碼斷言：diff 範圍三檔、api/ 與 geminiCache.ts 與其他 analyze* 零觸碰、PortfolioHealthItem 零改動。
</verification>

<success_criteria>
- 單檔健檢：decision 來自 json 機器區（emoji 前綴映射補回），Modal 顯示 cleanedMarkdown（無 json 區塊）；json 失敗時 regex fallback＋原文全文——舊行為是下限。
- 批次健檢：一顆「全部健檢」按鈕、一次 LLM 呼叫、逐檔 loading→done/error 三態、各檔各看各的切段報告＋總覽段附尾、切段失敗全文兜底。
- 同日重按批次 0 次 API（A3 快取 key 含 prompt hash，事實陳述、非本次實作）。
- 紅線全守：api/ 不動、其他 analyze* 不動、geminiCache 不動、型別相容、金鑰不進 bundle。
</success_criteria>

<output>
完成後建立 `.planning/quick/260713-2am-c-2-json-regex/260713-2am-SUMMARY.md`（含已知限制：>8 檔未 chunk）。
</output>
