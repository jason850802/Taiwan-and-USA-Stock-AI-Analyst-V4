---
phase: quick-260629-ag1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [AGENTS.md]
autonomous: true
requirements: [QT-ag1-AGENTS-POINTER]
---

<objective>
專案根目錄只有 `CLAUDE.md`（Claude 讀），沒有 `AGENTS.md`（Codex 讀）。因此 Codex 開啟本專案時只會讀到全域 `~/.codex/AGENTS.md` 的簡短基準規則，完全讀不到本專案的架構、安全紅線、程式風格、GSD 工作流程與 skills——Claude 與 Codex 在「這個專案」並未同步。

選項 A（單一事實來源）：新增 `AGENTS.md`，內容指向既有 `CLAUDE.md` 作為權威來源，並摘要最關鍵的紅線（GEMINI_API_KEY 安全、型別相容、程式風格、GSD 流程）。不複製 CLAUDE.md 內容，避免兩份不同步。
</objective>

<task>
1. 在專案根目錄建立 `AGENTS.md`：
   - 宣告「完整規則以根目錄 CLAUDE.md 為準」，本檔僅指向不複製。
   - 摘要紅線：GEMINI_API_KEY 不可進前端 bundle／git；資料領域型別相容；2 空格／單引號／繁中註解風格；改檔前走 GSD 流程。
   - 指出 `.claude/skills/`、`.agents/skills/` 的技能需先閱讀。
   - 註明專案層級優先於全域 `~/.codex/AGENTS.md` → CORE_RULES.md 基準。
</task>

<verify>
- 根目錄存在 `AGENTS.md`。
- 內容為「指向 CLAUDE.md」的指標檔，而非 CLAUDE.md 的複本。
- 明確涵蓋安全紅線與 GSD 流程。
</verify>

<done>
Codex 開啟本專案時，透過 AGENTS.md 會被導向 CLAUDE.md，與 Claude 看到同一套專案規則。
</done>
