---
phase: quick-260713-n11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [vite.config.ts, App.tsx]
autonomous: true
requirements: [D-1d, D-1e]
tags: [vite, rollup, manualChunks, code-splitting, react-lazy, bundle]

must_haves:
  truths:
    - "npm run build 產出多個 JS chunk：vendor（react+react-dom）、recharts、markdown（react-markdown+remark-gfm）與主 chunk"
    - "主 chunk raw 大小 ≤ 580.6 kB（較 D-1a 基線 967.60 kB 降 ≥40%，per D-1d 驗收硬指標）"
    - "vite.config.ts 既有 server proxy、plugins、resolve.alias 一字未變"
    - "若執行 lazy：Portfolio／FundamentalsPanel 切頁時有 Suspense fallback（Loader2 spinner），無白屏路徑"
    - "grep -r AIza dist/ 無結果"
  artifacts:
    - path: "vite.config.ts"
      provides: "build.rollupOptions.output.manualChunks 三分組"
      contains: "manualChunks"
    - path: "App.tsx"
      provides: "React.lazy 懶載非首屏分頁（若決策準則成立）"
    - path: ".planning/quick/260713-n11-d-1d-manualchunks-vendor-recharts-markdo/260713-n11-SUMMARY.md"
      provides: "chunk 大小全表（raw+gzip）、首屏 gzip 總量、lazy 決策記錄、D-1e 拍板記錄"
  key_links:
    - from: "vite.config.ts"
      to: "rollup output.manualChunks"
      via: "build.rollupOptions.output.manualChunks 物件形式"
      pattern: "manualChunks"
    - from: "App.tsx"
      to: "components/Portfolio.tsx"
      via: "lazy(() => import(...)) 動態匯入（若執行 lazy）"
      pattern: "lazy\\(\\(\\) => import"
---

<objective>
D-1d 分包：`vite.config.ts` 加 `manualChunks` 三分組（vendor／recharts／markdown），量測後依決策準則選配 React.lazy 懶載 Portfolio／FundamentalsPanel（非首屏分頁）；D-1e（Google Fonts 保留 CDN）零程式碼、記錄拍板進 SUMMARY。

Purpose: 主 chunk 較 D-1a 基線 967.60 kB raw 降 ≥40%（≤580.6 kB），改善首載與快取粒度（vendor/recharts 變更頻率低、可長期快取）。Phase D 4/7。
Output: vite.config.ts（commit 1）、App.tsx（若做 lazy，commit 2）、SUMMARY（不 commit）。
</objective>

<execution_context>
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/workflows/execute-plan.md
@D:/My Project/Taiwan-and-USA-Stock-AI-Analyst-V4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/optimization/PLAN.md（§D-1d :135-138、§D-1e :140 規格）
@.planning/quick/260713-kq2-d-1a-importmap-tailwind-cdn-bundle-class/260713-kq2-SUMMARY.md（§6 D-1d 錨點與三分建議）
@vite.config.ts
@App.tsx
@package.json
</context>

<preflight>
環境事實（worktree、Windows＋Git Bash）：

1. worktree 無 node_modules：先 `npm ci`。
2. 若 git 報 `dubious ownership`：`git config --global --add safe.directory "<worktree路徑>"`（前包 kq2 已踩過此雷）。
3. 量測用 Bash 工具（PowerShell 5.1 無 grep/gzip）。
4. 基準數字：D-1a 基線主 chunk **967.60 kB raw／294.26 kB gzip**；現行（D-1b/D-1c 後）單 chunk **958.72 kB raw／293.84 kB gzip**＋CSS 26.74 kB。驗收硬指標以 967.60 kB 為分母：主 chunk raw ≤ **580.6 kB**。
</preflight>

<tasks>

<task type="auto">
  <name>Task 1: vite.config.ts 加 manualChunks 三分組＋build 量測＋commit 1（D-1d 步驟 1）</name>
  <files>vite.config.ts</files>
  <action>
在 `vite.config.ts` 的 `defineConfig({...})` 內新增 `build` 區塊（與現有 `server`、`plugins`、`resolve` 平級），既有內容一字不動：

- `build.rollupOptions.output.manualChunks` 用**物件形式**，三分組照 optimization/PLAN.md §D-1d 規格：
  - `vendor`: `['react', 'react-dom']`
  - `recharts`: `['recharts']`
  - `markdown`: `['react-markdown', 'remark-gfm']`
- 不加其他 build 選項（不動 chunkSizeWarningLimit、不加 splitVendorChunkPlugin——物件形式已足夠且規格只列三分組）。
- 物件形式 rollup 會自動處理共享依賴歸屬（recharts 依賴 react → react 已顯式指派 vendor，不會重複進 recharts chunk），但需在 build 後驗證（見 verify）。

量測流程（Bash）：
1. `npm ci`（worktree 首次）
2. `npx tsc --noEmit`
3. `npm run build`——保留完整 build log（Vite 會印每個 chunk 的 raw＋gzip）
4. 逐 chunk 記錄精確 bytes：`for f in dist/assets/*.js; do raw=$(wc -c <"$f"); gz=$(gzip -9 -c "$f" | wc -c); echo "$f raw=$raw gzip=$gz"; done`
5. 重複打包檢查（兩法並用）：
   a. 全 JS chunk raw 總和 vs 現行單 chunk 958.72 kB——差異應在 ±10% 內（總和暴增 >10% ＝疑似重複打包，需查歸屬）
   b. `grep -lc "recharts-wrapper" dist/assets/*.js`——命中檔案數應恰為 1（recharts 專屬字串只出現在 recharts chunk）
6. `cat dist/index.html`——記錄 script（entry）與 `<link rel="modulepreload">` 清單（此時 vendor/recharts/markdown 應全部被 modulepreload——靜態 import 鏈使然，這是 Task 2 決策輸入）
7. `grep -r "AIza" dist/` 必須無結果

達標判定：entry 主 chunk（`dist/assets/index-*.js`）raw ≤ 580.6 kB。三 chunk 合理性直覺對照（非硬性）：vendor ≈ 140-220 kB raw、recharts 為最大宗（≈ 300-500 kB raw）、markdown ≈ 100-170 kB raw。

commit 1（僅 vite.config.ts）：`perf(d-1d): vite manualChunks 分包 vendor/recharts/markdown`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && bash -c 'raw=$(wc -c < $(ls dist/assets/index-*.js)); echo "main chunk raw=$raw bytes"; [ "$raw" -le 580560 ] && ! grep -r "AIza" dist/'</automated>
  </verify>
  <done>主 chunk raw ≤ 580,560 bytes（＝967.60 kB×0.6，Vite 報表為 SI kB／1000 進位，此閾值兩種進位下皆安全）；vendor/recharts/markdown 三 chunk 存在且大小記錄（raw＋gzip 全列）；recharts 字串單一 chunk 命中；JS 總和無暴增；vite.config.ts 既有 server/plugins/resolve 未動；grep AIza 無結果；commit 1 完成。</done>
</task>

<task type="auto">
  <name>Task 2: 依決策準則執行 React.lazy 懶載 Portfolio／FundamentalsPanel＋re-measure＋commit 2（D-1d 步驟 2 選配）</name>
  <files>App.tsx</files>
  <action>
**決策準則（per D-1d 選配規則，兩條件皆已預先偵察成立，執行時再確認）：**

1. Task 1 的 dist/index.html 檢查若顯示 vendor/recharts/markdown 全被 modulepreload（靜態 import 鏈 → 首屏 gzip 總量未因分包下降）→ 條件 1 成立。
2. lazy 改造只動 App.tsx：**偵察已確認** Portfolio（components/Portfolio.tsx:1524）與 FundamentalsPanel（components/FundamentalsPanel.tsx:167）皆為 default export、皆為 props-only（portfolioItems 等狀態全在 App.tsx，無跨分頁狀態提升需求）→ 條件 2 成立。

兩條件成立 → 執行 lazy。若執行時發現預期外阻礙（如 tsc 報 lazy 型別不相容需大改），記錄具體原因進 SUMMARY 並跳過本任務（不 commit 2）——這是規格允許的出口。

**改造內容（全部在 App.tsx）：**

1. 第 1 行 react import 加入 `Suspense` 與 `lazy`。
2. 刪除第 13-14 行靜態 import（`Portfolio`、`FundamentalsPanel`），改在 import 區塊之後宣告模組層級常數：`const Portfolio = lazy(() => import('./components/Portfolio'));`、`const FundamentalsPanel = lazy(() => import('./components/FundamentalsPanel'));`
3. 兩處分頁渲染（`currentView === 'portfolio'` 約 :339-346、`currentView === 'fundamentals'` 約 :348-350）各自包一層 `<Suspense fallback={tabFallback}>`。
4. fallback 沿用專案既有 Loader2 pattern（App.tsx:433-438 的載入覆蓋層樣式）：定義一個模組層級 `tabFallback` JSX——垂直置中容器（如 `className="flex flex-col items-center justify-center gap-2 py-24"`）內放 `<Loader2 className="animate-spin text-blue-400" size={32} />` ＋ `<span className="text-slate-300 text-sm">載入中…</span>`。Loader2 已在第 19 行 import，勿重複。**禁止空 fallback／null fallback（白屏）**。
5. 不動 AnalysisResult：react-markdown 經 MarkdownReport ← AnalysisResult（市場分析＝首屏分頁）靜態鏈進首屏，markdown chunk 續留 modulepreload 是**預期行為**，記錄實測即可，不為數字強行 lazy 首屏元件。

**re-measure（同 Task 1 流程 3-7）：**

- 重跑 build，記錄新 chunk 全表（Portfolio-*.js、FundamentalsPanel-*.js 應成為獨立 async chunk）。
- dist/index.html 應**不再** modulepreload Portfolio／FundamentalsPanel chunk（async chunk 不進首屏 preload 清單）；vendor/recharts/markdown 仍在（預期，記錄）。
- 計算並記錄**首屏 gzip JS 總量**＝entry ＋ 所有被 modulepreload 的 chunk gzip 加總（與 before ≈293.84 kB gzip、D-1a ≈294.26 kB 對照）。
- `npx tsc --noEmit`＋`grep -r "AIza" dist/` 無結果。
- 「分頁切換無白屏閃爍」的瀏覽器實測由 orchestrator 合併後驗；executor 驗證上限＝程式碼層確認 Suspense fallback 存在且非空。

commit 2（僅 App.tsx，獨立可回滾）：`perf(d-1d): React.lazy 懶載 Portfolio/FundamentalsPanel 非首屏分頁`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && bash -c 'grep -q "lazy(() => import" App.tsx && grep -q "Suspense" App.tsx && ls dist/assets/ | grep -iq "portfolio" && ! grep -r "AIza" dist/ && ! grep -i "portfolio" dist/index.html'</automated>
  </verify>
  <done>App.tsx 用 lazy＋Suspense（fallback 為 Loader2 spinner 非空白）；Portfolio／FundamentalsPanel 成為獨立 async chunk 且不在 dist/index.html 的 modulepreload 清單；首屏 gzip JS 總量記錄完成；tsc 過、grep AIza 無結果；commit 2 完成（或：決策準則未成立時，SUMMARY 記錄具體原因並跳過，無 commit 2）。</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 通過。
- `npm run build` 成功；主 chunk raw ≤ 580.6 kB（較 D-1a 967.60 kB 降 ≥40%）。
- vendor／recharts／markdown 三 chunk 存在；`grep -lc "recharts-wrapper" dist/assets/*.js` 恰 1 檔；JS 總和較 958.72 kB 無 >10% 暴增（無重複打包）。
- `grep -r "AIza" dist/` 無結果。
- vite.config.ts 既有 server proxy／plugins／resolve.alias 逐字保留（git diff 只見 build 區塊新增）。
- 若做 lazy：dist/index.html 無 Portfolio／FundamentalsPanel 的 modulepreload；Suspense fallback 存在且非空。
- commit 策略遵守：manualChunks 一 commit；lazy 一 commit（獨立可回滾）；SUMMARY 不 commit。
</verification>

<success_criteria>
- D-1d 驗收硬指標達成：主 chunk 較 967.60 kB raw 降 ≥40%。
- 首屏 gzip JS 總量（entry＋全部 modulepreload chunk 加總）記入 SUMMARY，與 before（≈293.84 kB gzip）對照。
- lazy 決策有明確記錄：做了（chunk 證據＋fallback 位置）或跳過（具體原因）。
- D-1e 記錄完成（見 output）。
</success_criteria>

<output>
寫 `.planning/quick/260713-n11-d-1d-manualchunks-vendor-recharts-markdo/260713-n11-SUMMARY.md`（**不 commit**，orchestrator 統一收尾），必含：

1. **chunk 全表**：Task 1 後與 Task 2 後（若做）各一份——每個 JS/CSS 檔的 raw＋gzip bytes 全列。
2. **-40% 達標計算**：主 chunk raw vs 967.60 kB 基線的降幅百分比。
3. **首屏 gzip JS 總量**：before ≈293.84 kB → after（entry＋modulepreload chunk 加總），並註明 markdown/recharts chunk 因 AnalysisResult／StockChart 靜態鏈續留首屏屬預期。
4. **lazy 決策記錄**：決策準則兩條件的實測結果＋做／跳過。
5. **D-1e 拍板記錄（零程式碼）**：Google Fonts 保留 CDN——display=swap 漸進降級可接受，不自托管；此為 optimization/PLAN.md §D-1e 既定決策，本包無新事證推翻。
6. requirements-completed: [D-1d, D-1e]。
</output>
